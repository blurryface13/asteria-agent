# Asteria Agent 开发规格

> 本文档用于记录项目目标、架构约束、接口契约和设计决策。开发节点、真实运行记录、额度统计与可执行任务计划统一维护在 [`DEVELOPMENT_LOG.md`](DEVELOPMENT_LOG.md)；实现过程中如果实际约束发生变化，先更新对应文档，再调整代码。

## 1. 项目目标

Asteria Agent 是一个本地优先的科研协作系统，当前重点是学术调研和综述报告撰写。系统需要从“能够按固定步骤生成报告”逐步发展为“能够理解任务、选择能力、调用工具、处理异常并交付可复现产物”的 Agent。

第一阶段不追求覆盖所有科研场景，优先把下面这条主线做扎实：

```text
科研问题 → 任务理解 → 研究计划 → 资料检索与阅读 → 证据整理
        → 报告撰写 → 引用核验 → 文件交付 → 任务复盘与继续执行
```

在不破坏学术调研主线的前提下，后续扩展到技术调研、数据分析、实验复现和其他需要长任务执行的场景。

## 2. 设计原则

### 2.1 稳定主线和自主决策并存

报告生成、引用归档和文件输出需要稳定、可调试、可回放；任务拆解、工具选择、补充检索、异常恢复和结果核验可以由 Agent 自主决策。不要为了体现 Agent 而把所有步骤都改成不可控的自由循环。

### 2.2 Skill 定义方法，Sub-Agent 执行角色，Tool 执行动作

三者职责保持清晰：

```text
Task Type → Skill → Sub-Agent → Tool / MCP
```

- `Task Type`：用户要完成什么类型的工作。
- `Skill`：这类工作采用什么方法、需要什么输入、产出什么结果、如何验收。
- `Sub-Agent`：在限定职责内做判断和执行。
- `Tool / MCP`：提供可验证的具体动作。

Sub-Agent 不需要和 Tool 一一对应。工具少且逻辑简单时可直接使用进程内 Tool；需要独立环境、跨项目复用或权限隔离时，使用 MCP。

### 2.3 所有长任务都必须可观察、可恢复

每次任务都应有独立的 Run ID，并保留：

- 当前阶段和已完成步骤
- Agent、LLM、Tool、Retriever 的调用轨迹
- 中间文本、结构化数据和文件产物
- 错误、重试和重规划记录
- 模型、Prompt、工具、数据和运行环境版本

浏览器关闭、单个工具失败或服务重启不应导致整个任务状态丢失。

### 2.4 Tool 执行必须受边界控制

任何文件读写、Shell、远程服务器和实验操作都必须经过用户配置的工作区、命令白名单、超时和权限策略。Agent 只能提交结构化任务，不能直接获得任意命令执行能力。

### 2.5 每项功能先验证，再提交

后续开发按以下节奏进行：

```text
小功能 → 单元测试/类型检查 → 本地启动 → 固定任务验证 → 检查 Trace/产物 → commit → push
```

每个 commit 尽量只解决一个清晰问题，避免在一个提交中同时改变编排、存储、前端和运行环境。

## 3. 当前项目状态（2026-09-10）

### 3.1 已有能力

- Python 调研引擎位于 `asteria_researcher/`，负责查询拆解、检索、抓取、上下文处理、引用和报告生成。
- 基于 LangGraph 的多 Agent 编排位于 `multi_agents/`。
- 当前多 Agent 角色包括 Researcher、Editor、Writer、Reviewer、Reviser、Fact Checker、Visualizer 和 Publisher。
- 主多 Agent 链路已经包含大纲规划、人工审核、章节并行研究、写作、事实检查、可视化和发布。
- `ResearchAgent` 已支持按 perspective 生成补充研究视角并行调研。
- MCP 已接入客户端管理、工具发现、工具选择、工具调用和结果归一化。
- MCP 路径支持关闭、快速复用和逐子查询深入执行等策略。
- 项目已有 WebSocket 流式日志、FastAPI 服务和 Next.js 前端。
- PostgreSQL 已通过幂等启动初始化保存报告、项目、对话和消息；Workspace API 按用户隔离项目/对话层级，报告与 conversation 当前使用稳定 ID 对齐。
- 已有初步评测模块：任务、Trace、Scorer、BadCase、SeedCase、GeneratedCase、Runner 和本地存储。
- 已有独立的评测文档和示例 Trace，可作为后续 Agent 评测的基础。
- 报告当前主要输出 Markdown、Word 和 PDF；尚未形成完整的 LaTeX 工作区和模板体系。

### 3.2 当前编排的真实边界

当前系统是“多 Agent Workflow + 局部 Agent 化能力”，还不是完全开放式的自主 Agent：

- `ChiefEditorAgent` 负责固定主图，节点顺序基本确定。
- `EditorAgent` 负责生成章节，`ResearchAgent` 负责章节研究，章节之间可以并行。
- `Reviewer`、`Reviser` 和 `FactChecker` 存在条件回路，但主要围绕预先定义的报告流程工作。
- MCP 工具选择和 perspective research 已有动态决策，但还没有统一的任务级重规划和停止策略。
- 现有角色没有全部形成“独立状态、专属工具集合、输入输出契约和失败边界”。
- 输出目录能够保存产物，但任务状态、运行状态、轨迹和文件尚未统一为一个可恢复的长任务模型。

因此，下一步不是简单增加更多 Agent，而是补齐任务状态、能力注册、工具边界、重规划和产物管理。

### 3.3 Phase 1 实现进度

- [x] 增加 `TaskSpec`、`IntentResult`、`SkillManifest`、`AgentProfile` 和 `ToolSpec` 契约。
- [x] 增加内存版 `CapabilityRegistry`，注册当前科研路径和未来实验、离线 RAG、数据分析扩展点。
- [x] 增加不依赖外部模型的 `IntentRouter`，用户不需要手动选择任务类型。
- [x] 为现有 Editor、Researcher、Writer 和 Fact Checker 建立初始 Agent Profile。
- [x] 为意图路由、Skill 解析、任务构造和重复注册增加单元测试。
- [ ] 将 Intent Router 接入 API 请求和现有主编排入口。
- [ ] 用结构化 LLM 分类器替换或增强规则路由，并保留确定性回退。

### 3.4 本轮前端与启动状态

- [x] 首页与研究任务页拆成两个明确状态：初始页保留直接输入和历史研究入口；提交任务后进入任务工作台。
- [x] 首页提供独立的“进入工作台”入口，空输入时发送按钮禁用，进入工作台不会创建任务或打开检索连接。
- [x] 首页和研究工作台开始统一为深色任务界面：左侧常驻工作区导航，中间任务/报告主区，底部 composer，减少说明性 Hero 文案。
- [x] 左侧导航支持完全收起和恢复，恢复按钮始终保留在主区左上角；窄屏使用抽屉与遮罩。知识库、RAG、文档编辑、评测保留在“更多工具”中。
- [x] 默认研究结果页的报告、来源、日志、问答和图片卡片已切换到统一的深色半透明视觉层，保留原有数据组件和交互。
- [ ] 继续将结果页的 transcript、步骤、子 Agent、产物和报告问答改造成统一任务工作台；不改变现有研究 API 和 report chat 语义。
- [x] 研究 WebSocket 在连接超时、异常关闭或服务端错误事件到达时结束 loading 状态，避免把后端异常伪装成无限检索。
- [x] 设置弹窗改为客户端加载，避免 Portal 与动画依赖在 Next.js 开发态服务端渲染时阻塞首屏。
- [x] 本地启动支持显式免登录模式，前端 AuthGuard、后端 HTTP API 和 WebSocket 使用同一组本地开发开关；生产/共享环境默认不绕过鉴权。
- [x] 确认 `Hero`、工作台布局、`/login` 和根页面可独立完成首次编译并返回 200。

本轮检索故障记录：真实任务能够完成网页抓取和报告生成，但日志暴露出两个独立问题。其一，当前网络路径下部分外部检索站点和模型端点出现连接重置、TLS 握手中断或超时，原实现的非流式 LLM 请求最多重试 10 次，导致用户侧长时间停留在 loading。其二，`ollama:bge-m3` 在本机 Ollama 0.23.2 上调用 `/api/embed` 时触发 Metal `failed to create command queue`，本地 embedding runner 直接退出；该异常在每个子查询中被记录后继续，造成上下文压缩阶段失去 dense 信号。当前已将 LLM 重试预算收敛为可配置的 3 次、统一服务端错误事件，并补齐前端错误终止逻辑；这只解决任务状态不透明问题，不能替代对网络代理和 Ollama 运行时的环境修复。官方后续版本已包含 macOS 26 相关 Metal 修复，项目启动脚本会要求不低于 0.23.3 并执行真实 embedding 自检。后续固定任务验证必须同时记录外部端点连通性、embedding 健康检查、检索结果数量和报告产物，不能只以页面停止转圈作为成功标准。

本轮排查记录：修改组件时曾先删除再重建多个文件，Next.js 热更新短暂出现 `Cannot find module`；随后旧开发进程还出现根页面请求长期不返回。干净进程逐页验证后，确认阻塞点来自设置弹窗在服务端渲染阶段的 Portal/动画依赖，而不是研究编排或后端服务。验证期间并行启动多个 Next 实例又触发了 macOS `EMFILE`，使 `build`、`lint` 和 `tsc` 出现长时间无输出；这类命令不能与开发服务器或彼此并行运行。今后前端改动遵循“先新增/替换后删除临时文件、单端口干净启动、逐页 HTTP 检查、再进行浏览器交互验证”的顺序；发现端口已监听但页面不返回时，以实际页面响应和日志为准，不直接复用旧热更新进程。

本轮登录循环排查记录：旧页面曾因失效 JWT 或前后端鉴权配置不一致收到 401/4401，原前端会在 HTTP 与 WebSocket 两条路径清理本地 token 并强制跳转 `/login`；同时本地免登录开关只存在于某次启动命令的进程环境中，手动启动 Next 或复用旧标签页时容易再次落入登录页。现已统一由 `isLocalAuthBypassEnabled()` 判断本地模式，固定补入前端 `.env.local`/`.env.example` 与启动文档；明确开启本地模式时，登录页会回到首页，401/4401 会保留真实错误并提示检查后端开关和 API 地址，不再形成登录循环。非本地模式仍保持失效 token 清理和登录跳转。验证方式为：干净重启 Next、直接访问 `/login`、刷新根页面，并请求 `/api/reports`；三者均通过后再测试 WebSocket，不能只看地址栏是否变化。

本轮任务/问答边界排查记录：参考端到端科研产品的交互后，确定首页 composer 的语义是“创建长任务”，任务工作区内的 composer 才是“围绕当前交付物继续追问”；知识库问答属于独立上下文，不应复用新建任务入口。原实现的 `handleChat()` 在移动端没有报告时会调用 `handleDisplayResult()`，导致 Chat 输入被误转成新的调研任务。现已删除这条调用路径：研究报告问答必须存在报告上下文，没有上下文时只提示用户进入知识库入口；工作区在报告完成前也不展示 Ask report 按钮。后续新增全局 Chat 或意图路由时，仍需保留 `research/create`、`report/chat`、`knowledge/ask` 三种明确命令语义，不能仅凭同一个输入框状态猜测。

参考交互原则：任务页应以持续 transcript 为主，用户输入、Agent 思考、工具动作、文件产物和最终报告都属于同一任务时间线；步骤、子 Agent、主机和用量是辅助检查面板，不应打断主线。当前前端按深色工作台、半透明层次和紧凑信息密度重组，同时保留本地项目的研究品牌；后续优先补任务状态时间线、工具调用明细、报告/知识库上下文标识和可恢复的任务输入。

## 4. 目标架构

```text
Frontend
  ├── New Task / Task List / Project
  ├── Live Steps / Trace / Sub-Agents
  ├── Artifacts / Report / Logs
  └── Hosts / Skills / Memory Settings
          ↓
Research API
  ├── Task Manager
  ├── Run Manager
  └── Event Stream
          ↓
Agent Orchestrator
  ├── Task Planner
  ├── Skill Router
  ├── Sub-Agent Runtime
  ├── Replanner / Recovery
  └── Completion Checker
          ↓
Capability Layer
  ├── Literature Research Skill
  ├── Systematic Review Skill
  ├── Technical Survey Skill
  ├── Experiment Skill
  ├── Data Analysis Skill
  └── LaTeX Report Skill
          ↓
Tool Layer
  ├── Search / Browser / Paper Reader
  ├── Knowledge Base / MCP
  ├── Workspace / Artifact
  ├── Experiment Worker
  └── Citation / Report Validator
          ↓
Trace Store / Artifact Store / Memory / Evaluation
```

## 5. Agent、Skill 和 Tool 设计

### 5.1 首批能力类型

先支持以下能力，未来可以扩展其他任务类型：

| Task Type | 目标 | 主要能力 |
| --- | --- | --- |
| `literature_review` | 文献调研与综述 | 检索、阅读、证据整理、报告撰写 |
| `technical_survey` | 技术路线与方法比较 | 论文/文档检索、方法抽取、对比分析 |
| `systematic_review` | 规范化综述 | 检索协议、筛选、去重、证据表、引用检查 |
| `experiment` | 可复现实验 | 环境选择、命令执行、指标读取、产物归档 |
| `data_analysis` | 数据分析与图表 | 数据读取、代码运行、统计分析、图表生成 |

股票或公司调研可以作为后续新增的 `company_research` 类型，不应提前把当前科研主线硬编码成唯一任务形态。

### 5.2 首批 Sub-Agent

- `PlannerAgent`：理解任务，生成目标、阶段、约束和完成条件。
- `ResearchAgent`：执行检索、阅读和证据整理，拥有受控的研究工具。
- `EvidenceAgent`：建立结论与证据、来源和引用之间的关系，发现支撑不足和来源冲突。
- `AnalystAgent`：处理数据、运行分析代码和解释实验结果。
- `WriterAgent`：根据结构化研究结果撰写报告，不负责自由决定全部工具。
- `ReviewerAgent`：按 Skill 的验收标准检查报告和产物，必要时触发重规划。
- `ExperimentAgent`：生成和提交结构化实验任务，不能绕过工作区和命令权限。
- `ArtifactAgent`：管理 Markdown、LaTeX、图片、表格、日志和指标文件。

现有角色优先通过适配和职责收敛复用，不先重写。`ChiefEditorAgent` 后续逐步演化为 Orchestrator，`EditorAgent` 的规划能力可以迁移到 `PlannerAgent`，`ResearchAgent`、`WriterAgent` 和 `FactCheckerAgent` 保留为初始实现。

## 6. 工具与 MCP 规划

### 6.1 研究工具

第一阶段保留已有工具，并统一工具契约：

- 学术搜索、网页搜索和来源读取
- 文献内容抽取与摘要
- 知识库检索
- 引用和来源记录
- 工作区笔记读写

工具统一声明名称、描述、JSON Schema、超时、重试策略、权限等级和返回结构。MCP 结果先归一化成项目内部的 `EvidenceItem`，避免下游 Agent 依赖某个 MCP 服务的原始返回格式。

### 6.2 MCP 使用边界

- 进程内的纯函数和轻量校验逻辑，优先使用 LangChain Tool。
- 知识库、远程执行、实验环境和需要跨 Agent 复用的能力，优先使用 MCP。
- 不把所有内部函数都暴露成 MCP Tool。
- Orchestrator 只看到少量高层能力，底层工具由对应 Skill 或 Sub-Agent 管理。

## 7. 受控实验执行

实验功能是独立能力，不与普通文献检索混在同一条无边界工具链中。

### 7.1 用户配置的运行环境

每个 Host Profile 至少包含：

```yaml
name: local-gpu
host: local
workspace: /path/to/project
python_env: dora
allowed_commands:
  - python
  - pytest
  - uv
network_policy: disabled
timeout_seconds: 3600
approval_required: true
```

远程主机额外记录 SSH 别名、远程工作区、GPU 信息和文件同步策略。凭据不进入任务 Prompt、Trace 或 Git。

### 7.2 Experiment Recipe

可复用实验不由 Agent 临时拼装成不可复现的命令，而是使用版本化 Recipe：

```yaml
name: reproduce_baseline
inputs:
  - dataset
  - checkpoint
  - epochs
steps:
  - command: python train.py --config configs/baseline.yaml
  - command: python evaluate.py --checkpoint outputs/best.pt
outputs:
  - outputs/metrics.json
  - outputs/figures/
validation:
  - outputs/metrics.json exists
```

执行器负责校验工作区、命令、参数、超时和输出；Agent 负责选择 Recipe、填写参数、解释结果和决定是否需要下一轮实验。

### 7.3 实验产物

每轮实验至少归档：

- Recipe 版本和实际命令
- 代码、数据和环境版本
- 标准输出、错误日志和退出状态
- 指标 JSON、图表和检查点路径
- Trace、Artifact Manifest 和运行时间

## 8. 报告与 LaTeX

LaTeX 采用“结构化内容 + Skill + 模板 + 确定性渲染”的方案，不让 LLM 直接自由生成整份排版源码。

### 8.1 报告中间结构

Writer 输出结构化报告对象：

```json
{
  "title": "...",
  "abstract": "...",
  "sections": [],
  "tables": [],
  "figures": [],
  "citations": [],
  "appendix": []
}
```

### 8.2 模板目录

```text
templates/
├── survey/
│   ├── template.tex
│   ├── preamble.tex
│   ├── template.yaml
│   └── bibliography.bib
├── technical_report/
└── conference_paper/
```

模板负责页面、字体、章节、图片、表格、公式、参考文献和附录样式；Skill 负责选择模板、填充内容和触发编译。

### 8.3 编译与校验

LaTeX Skill 需要支持：

- `latexmk` / XeLaTeX 编译
- 引用键存在性检查
- 图片路径和格式检查
- 表格、公式和交叉引用检查
- 编译日志归档
- 编译失败后的定向修复
- `.tex`、`.bib`、图片、PDF 和日志一并交付

### 8.4 综述写作 Skill 与工具边界（2026-09-11）

报告写作从文献研究 Skill 中独立出来，形成 `report_writing`。研究 Skill 负责“是否继续研究、哪些证据足够”，写作 Skill 负责“如何组织已确认的证据并交付报告”，两者不互相改变停止条件。

写作 Skill 的固定约束包括：按问题和比较维度综合，不逐篇堆叠论文；区分原文证据、跨论文综合和未解决限制；引用只能来自当前任务实际读取的来源；对实验数字同时保留数据集、指标和条件；输出使用兼容确定性渲染的 Markdown，不直接执行模型生成的 LaTeX。

能力注册表新增三类工具契约：`report_structure_check` 检查标题和交付结构，`citation_audit` 将报告链接与已读取来源匹配，`latex_compile` 使用固定安全模板编译并归档日志。当前这些工具先作为进程内能力复用，不改变顶层 Agentic 编排；后续再将 BibTeX、结构化表格/公式和页面级定位接入同一契约。

外部参考采用“机制借鉴、代码不直接移植”的方式：高校工具链的证据台账、引用核验、前置文献追溯和引用关系图可转化为本项目 Tool；学术写作项目的细粒度 Skill 和 Human-in-the-loop 反馈可转化为本项目的写作校验。具体来源和许可证在引入任何代码前单独核查。

## 9. 前端目标

前端首先服务于任务执行和过程理解，不先追求复杂的社交、订阅或全平台能力。

### 9.1 信息架构

- 新任务：直接描述目标，由意图路由识别任务类型，再补充 Skill、模型、工作区和输出格式。
- 项目：一个目录承载相关任务、资料和产物。
- 任务：展示状态、步骤、运行记录和历史版本。
- 主机管理：维护本地或远程 Host Profile。
- Agent：维护身份、能力和工具权限。
- Skill：查看方法说明、输入输出契约和可用工具。
- 记忆：管理项目级研究偏好、术语和长期事实。

### 9.2 任务工作区

任务页面至少分成三块：

```text
左侧：项目与任务导航
中间：对话、步骤流和实时执行过程
右侧：概览、子 Agent、工具、文件、日志和用量
```

重点交互包括：

- 查看当前步骤和下一步计划
- 展开某次 Tool Call 的参数、结果和错误
- 暂停、继续、重试和从某一步重跑
- 查看生成的报告、代码、图片、数据和 PDF
- 对局部产物提出修改要求

视觉上统一采用中性炭灰工作台：导航背景、任务主区、输入框三层明度，细边框与大圆角，不再叠加蓝黑渐变或玻璃卡片。初始页使用简短问候、场景示例与底部输入框；任务页使用导航、执行记录和右侧产物面板。系统字体承载操作控件，示例卡片标题使用中文衬线字体。弹窗的背景模糊只用于区分当前操作层。

## 10. 评测方向（第二阶段）

评测系统暂不阻塞第一阶段的 Agent 功能，但执行层从现在开始必须保留足够的 Trace。

后续评测由两部分组成：

1. 科研任务和报告质量：参考 DeepResearch Bench，评估任务完成、证据覆盖、引用支撑和报告质量。
2. Agent 行为和工具轨迹：参考 TRAJECT-Bench，评估工具选择、参数、调用顺序、异常恢复和多次运行稳定性。

当前已有的 `Trace`、`EvaluationRunner`、`Scorer`、`BadCaseAnalyzer` 和种子题库先保留。后续重点补充：

- 固定任务集和版本信息
- claim–evidence–citation 级别的引用核验
- 工具参数、调用顺序和重规划评分
- 离线回放、重复运行和 `pass^k`
- BadCase 人工确认、样本生成和回归门禁

## 11. 分阶段计划

### Phase 0：基线确认

- 固化当前启动方式和开发环境。
- 跑通普通报告、多 Agent 报告和 MCP 研究路径。
- 检查 Trace、报告和输出目录。
- 不改变现有用户可用主流程。

### Phase 1：任务与能力模型

- 增加 Task Type、Skill Manifest 和 Agent Profile。
- 将现有角色和工具映射到能力模型。
- 统一工具 Schema、结果结构和错误类型。
- 增加 Run ID、任务状态和事件模型。

### Phase 2：局部 Agent 化

- 增加任务级 Planner。
- 支持动态补充检索、工具切换和有限重规划。
- 增加明确的完成条件、预算和停止条件。
- 保留稳定报告 Workflow 作为可复现模式。

### Phase 3：可恢复任务与工作区

- 将长任务执行从 API 请求中解耦。
- 保存中间状态和 Artifact Manifest。
- 支持暂停、继续、失败重试和从指定步骤恢复。
- 前端展示步骤、子 Agent、工具和文件变化。

### Phase 4：受控实验能力

- 增加 Host Profile 和权限策略。
- 增加 Experiment Recipe 和 Experiment Worker。
- 支持本地 dora 环境和用户配置的远程主机。
- 归档命令、环境、指标和实验产物。

### Phase 5：结构化报告与 LaTeX

- 增加报告中间结构。
- 增加综述和技术报告模板。
- 实现 LaTeX 渲染、编译和校验。
- 前端支持源码、PDF、图表和编译日志查看。

本阶段新增细分顺序：先完成 `report_writing` Skill 和确定性报告校验，再引入综述模板；综述模板稳定后，才区分 `literature_review` 与 `conference_paper` 的写作契约。

### Phase 6：评测和持续优化

- 建立科研 Golden Set、Adversarial Set 和 BadCase Set。
- 接入报告质量和轨迹级评分。
- 建立版本对比、回归门禁和趋势报告。
- 用评测结果反向优化 Prompt、Skill、工具编排和 Agent 策略。

## 12. Commit 计划

每完成并验证一个阶段性能力，再单独提交：

1. `docs: add agent architecture and development spec`
2. `feat: add task type and skill capability model`
3. `feat: unify tool contracts and agent profiles`
4. `feat: add task planner and bounded replanning`
5. `feat: persist resumable runs and artifacts`
6. `feat: add host profiles and experiment recipes`
7. `feat: add controlled experiment worker`
8. `feat: add structured report and latex renderer`
9. `test: add research task smoke and regression cases`
10. `docs: add deployment, experiment, and evaluation guides`

如果某个功能需要多次迭代，优先拆成“模型/接口—实现—测试—前端”的连续小提交，确保每个节点都能启动和验证。

## 13. 当前不做的事情

- 不在第一阶段实现 PPT、团队订阅、商业化和多端同步。
- 不为了增加 Agent 数量而重写已有 LangGraph 主线。
- 不把所有工具一次性暴露给顶层 Agent。
- 不允许 Agent 直接执行未授权的任意 Shell 或远程命令。
- 不在评测指标尚未稳定前宣称绝对质量提升。
- 不因为引入新能力而破坏当前普通报告生成路径。

## 14. 第一阶段完成标准

第一阶段至少应满足：

- 普通科研报告仍可正常生成。
- 多 Agent 报告仍可正常运行。
- Agent 能根据任务类型选择对应 Skill。
- 工具调用、失败、重试和重规划能够在 Trace 中看到。
- 任务可以保存中间状态并继续执行。
- 用户可以为实验指定主机、工作区、命令和时间边界。
- 实验结果和报告文件能够归档到任务工作区。
- 至少有一种综述报告能够通过固定 LaTeX 模板编译。
- 每个阶段性功能都有测试、运行记录和独立 commit。

## 15. 工作台实现记录与接入契约（2026-09-10）

### 15.1 本次前端边界

`frontend/nextjs/components/harness/` 承载新版工作台，根页面仍管理现有调研状态。保留按屏幕宽度选择不同请求链路的既有产品决策，本次不统一移动端与桌面端后端接口，也不替换模型、环境、鉴权或研究编排。

已接入：任务输入、原有调研与报告问答回调、历史报告读取、真实执行日志、检索策略设置、报告 Markdown 预览/源码/下载。历史列表点击后在当前工作台展示，原有直接报告链接保持兼容，独立报告路由的视觉迁移单独安排。

已持久化：项目、conversation、消息和报告均由 PostgreSQL 保存；新研究提交时读取当前项目并立即建立同 ID 的 conversation，首条问答和后续报告问答写入 Workspace 消息表。项目详情面板可以在当前项目中新建任务并查看其子任务状态。`localStorage` 仅用于显示名称、当前项目选择、首屏缓存和旧草稿迁移，不作为已保存任务的唯一来源。

界面占位：主机、目录浏览、SSH、人格、记忆、Skill 管理、任务附件、语音、团队订阅、轨迹导出及 LaTeX 编译。所有占位均明确标识待接入；不展示虚构工具耗时、Token 用量、子 Agent 状态或执行成功。现有断开按钮仍沿用原停止处理，不宣称服务端取消、暂停或可恢复。

首页分类只是可编辑提示词示例，不是强制 Task Type。用户可以直接输入任意目标；选择例子不会立即调用模型。PPT 不在本期范围，科研示例使用论文精读与实验计划。

### 15.2 下一阶段接口锚点

| 工作台位置 | 后端资源 / 命令（拟定） | 验收边界 |
| --- | --- | --- |
| 项目与任务列表 | Project、Task、Run | 项目绑定授权工作区；同一任务支持多次 Run，不把报告文件 ID 当作 Run ID |
| 主机与目录选择 | HostProfile、WorkspaceGrant | 服务端规范化路径并校验目录边界；拒绝符号链接越界；前端不保存 SSH 私钥 |
| Agent 配置 | AgentProfile、SkillManifest | 版本化工具白名单、输入输出契约、停止条件；仅暴露实际可用能力 |
| 记忆 | MemoryEntry、MemoryProposal | 区分用户偏好、项目事实、可复用经验；来源可查、支持删除，不自动存入秘密或未验证结论 |
| 任务过程与右侧面板 | RunEvent、ToolCall、Artifact | 流式事件带序号；断线后按序补齐，去重回放；展现决策摘要，不要求输出模型隐式思维链 |
| 任务操作 | cancel、retry、resume、approve | 命令需服务端确认；客户端断开不等价于服务端任务终止 |
| 报告预览 | ArtifactVersion、CompileJob | 同时保留 Markdown、结构化报告、引用、LaTeX/PDF 和编译日志，失败可定位 |

当前已落地的 Workspace API：`/api/workspace/projects`、`/api/workspace/conversations` 和 `/api/workspace/conversations/{id}/messages`，支持项目/对话/消息的创建、查询、修改和删除；项目筛选使用 `project_id`，所有查询按当前用户邮箱隔离。正式的报告索引/外键、从 conversation 直接恢复完整报告视图和项目内任务分组仍按独立 commit 推进。

事件建议字段：`event_id`、`run_id`、`sequence`、`parent_event_id`、`agent_id`、`kind`、`status`、`timestamp`、`payload`、`artifact_refs`。敏感参数进入日志前必须脱敏。状态区分 `queued/running/waiting_approval/completed/failed/cancelled`，重试创建新的 attempt，不覆写旧失败记录。

### 15.3 从固定流程向 Agent 扩展

1. **保留成熟能力作为工具。** 将现有章节研究、检索、报告生成封装为可调用能力，而不是重写其内部流程。顶层协调者先判断是直接回答、引用报告/知识库、补充研究还是执行实验。
2. **按证据缺口调整计划。** 协调者维护目标、已知证据、未解决问题与预算。只有检索不足、来源冲突、工具失败或验收未通过才新增步骤、换检索方式或派发子 Agent；设置最大重规划次数、调用预算与截止时间。
3. **细粒度 Skill 组合。** 文献筛选、论文精读、方法比较、引用核验、数据口径核验、实验设计、LaTeX 排版是独立 Skill；任务可组合多个 Skill，按需加载，不向每个 Agent 注入全部说明。
4. **隔离子任务上下文和权限。** 子 Agent 接收问题、必要证据、允许工具、预算和输出契约；返回结果、证据引用、产物及未解决事项。并行研究共享证据索引而非互相追加完整历史。
5. **实验执行先过授权门。** 用户指定主机、目录、环境和资源预算；Recipe 固化可复用命令。安装依赖、覆盖文件、远程写入或长时 GPU 任务必须按风险要求确认。工具失败显式返回，不伪装成成功。
6. **在线成果由用户选择入库。** 报告与检索证据先作为任务产物，确认后进入离线知识库，记录来源、时间、版本和引用关系。报告问答、离线问答和新调研必须显示各自上下文，不互相静默升级。

实现顺序：持久化 Run/Event 与状态恢复 → 有预算的协调者和能力选择 → 子任务边界与证据索引 → 受控实验 → LaTeX 产物链路 → 轨迹/报告评测。每步单独测试、提交，不把全部规划一次性实现。

### 15.4 本轮验证口径

- Node 22 下执行 TypeScript 检查；补齐现有 `logs` 事件类型，未通过类型断言隐藏问题。
- 实际浏览器检查桌面与窄屏、侧栏收放、弹窗 Escape、场景填入、空输入进入工作台和占位状态。
- 保持 3023 前端与 8018 后端既有进程，不清理运行中的 `.next`，不重新安装依赖或另开 Next 实例。
- 本轮 UI 验证不等价于完整在线调研或模型质量回归；真实模型调用、远程工具、LaTeX 和恢复机制应各自记录专项测试结果。

## 16. 科研能力第一轮实现：综述与复现实验设计

本节覆盖第 15 节中相关占位的最新状态。个人工作区删除 Free 标签，不引入订阅等级。

### 16.1 技术路线与实际复用点

新增 `asteria_researcher/agentic/runtime.py`，作为现有研究引擎之上的有界协调层。不是重写检索器，也不是用更多固定角色替换原 LangGraph 主图。`backend/server/agentic_runner.py` 将既有 `BasicReport`（含其已有检索、上下文与多视角能力）作为隔离的研究工具，复用现有模型配置、MCP 配置和请求级参数。

交互式 WebSocket 中出现明确“文献综述/综述”或“复现实验”交付意图时进入新协调层；普通研究、REST、移动端与外部 LangGraph 路径保持原接口。本期路由是窄范围关键词规则，不宣称已经有通用语义意图分类器。前端场景卡片只填入自然语言，不强制选择 Task Type。

执行链路：

```text
自然语言 → 加载 Skill → 结构化范围/视角计划 → 人工确认或修改
        → 分视角 literature_search → evidence_audit
        → 按缺口补充研究（可选）→ 科研写作 → 引用集合/方案字段检查
        → 安全模板生成 LaTeX → XeLaTeX 编译 → Markdown + .tex + PDF + 日志
```

模型可决定初始视角与补充研究问题、是否需要补充证据；工具集合和执行预算由代码控制。当前研究视角顺序执行，避免已有日志写入与模型配额的并发问题；这不是新建了并行子 Agent 运行时。后续可在独立事件存储与并发预算准备好后并行化。

### 16.2 Skill 与验收

- `agentic/skills/literature_review.md`：范围与筛选标准、方法分类、证据比较、局限、研究空白、参考文献。不允许把不同实验条件下的数字直接视为方法优劣。
- `agentic/skills/experiment_design.md`：先核查原论文/公开实现，再明确基线、数据、环境、指标、验收、消融和失败分析。交付物是**实验设计**，不会执行命令或训练，不编造运行结果。
- 当前引用检查确认报告 URL 来自本轮研究证据，不等于验证每一条科学论断，也不能证明研究工具本身没有生成错误引用。后续应增加论文元数据/DOI 核对与 claim-evidence 对齐。
- 方案字段检查当前是必需关键词检查，不等于完整结构化协议验证。后续将逐项升级为有单位、有来源、有阈值类型的 Protocol Schema。

### 16.3 预算、失败与人工确认

初始视角最多 3 个；最多 2 轮研究、5 次研究工具调用；一次任务总超时 1800 秒；每次人工确认超时 600 秒；计划最多修改 2 次。审阅仍要求补研但预算耗尽时停止交付；允许交付的报告也必须保留审阅列出的局限。写作后的引用/字段校验允许一次定向修订，之后仍不满足就失败。空证据、无来源、非法 JSON 或编译失败都显式失败，不伪造成功产物。当前没有服务重启恢复能力。

修复人工确认传输：`handle_websocket_communication` 是唯一接收者，将反馈送入请求级队列；`CustomLogsHandler.request_feedback` 等待队列，研究任务运行时不再丢弃反馈。原 HumanAgent 在此传输下复用该方法。前端移除冻结首次 hook 结果的 ref，发送确认使用最新 socket；确认内容改为字符串类型。

中间研究工具的 `report/report_complete/path/research_report` 事件不作为最终交付转发，避免 UI 提前完成。最终产物只在校验与编译完成后公布。

### 16.4 LaTeX 交付与安全边界

`agentic/latex.py` 使用 ctexart/Fandol 中文模板。模型产生 Markdown，程序转义后生成 TeX；不执行模型直接给出的 TeX 宏。编译通过固定参数启动 XeLaTeX，关闭 shell escape，限制文件读写策略，设置 90 秒超时，失败保存编译输出。不支持任意 LaTeX 编辑后执行、复杂数学/表格的完整 Markdown 转换，后续用结构化报告节点逐步替换。

产物保存于 `outputs/scientific_<uuid>/`，包括 `report.md/report.tex/report.pdf/compile.txt`。沿用现有 outputs 静态访问机制，本地个人模式使用；共享部署前必须增加用户/任务权限校验。前端从后端 path 事件获取路径，严格限定新产物目录格式，在报告面板的 LaTeX 入口并排查看源码/PDF及编译日志。

### 16.5 后续模型接续开发清单

1. 先运行 `dora/bin/python -m pytest tests/test_agentic_runtime.py tests/test_agentic_transport.py tests/test_capabilities.py -q`，再运行前端类型检查。
2. 保留 3023/8018 既有服务端口与 Node 22/dora；更新后端通过拥有的终端会话正常停止并重启，不再另开一组服务。
3. 增强证据工具：论文元数据检索、原文段落引用、条件可比性校验。不要用 URL 集合检查冒充事实验证。
4. 增强结构化 Protocol 与 LaTeX：表格/公式/图表节点、BibTeX、编译定位；不开放任意宏执行。
5. 再接独立 Run/Event/Artifact 存储、事件游标与恢复；随后接授权 Host/Workspace/Recipe 执行，当前仅设计实验。
6. 最后增加评测：规划通过率、证据覆盖、引用支持率、预算遵守、反馈恢复、方案可执行性；与原研究路径按固定任务集对比。

当前自动化覆盖正常交付、证据缺口补研、人工修改、不自动批准、未知引用拒绝、实验方案字段、TeX 转义、真实中文 PDF 编译及运行中反馈队列。真实模型 Planner 的 JSON 契约已单独调用验证；完整在线研究需另记运行结果，不从单测推断完成。

### 16.6 原文工具与启动排障记录（2026-09-11）

`agentic/primary_sources.py` 提供独立原文读取能力。对于用户明确给出的学术站点链接，优先读取原文，arXiv abs 地址转换为同论文 PDF 表示；没有明确论文链接时仍使用既有研究引擎发现来源。原文按视角提取证据，任务内缓存避免重复下载。论文内引用的其他文献不自动升级为已读取来源。

访问范围为显式 HTTPS 学术站点白名单，每次重定向重新校验；不接受用户信息或非标准端口。下载上限 12 MiB、最多 3 次重定向、工具总超时 60 秒；最多读取前 30 页/65000 字符，返回截断边界。扫描版无文本直接报错，当前不含 OCR。证据提取仍经过模型，不等价于逐句事实核验。

本机曾出现 arXiv 证书链无法验证，导致旧网页读取器返回空证据。修复限定在该工具的 HTTP 客户端：使用 `truststore.SSLContext` 获取系统信任库，保持证书校验，不改代理或全局 SSL 配置。直接依赖 `truststore>=0.10`，PDF 提取复用已有 PyMuPDF，不另装一套 PDF 环境。

环境沿用 dora Python 3.10 与 Node 22。Python 3.10 的总超时使用 `asyncio.wait_for`。dora 补齐既有声明依赖 tavily-python 0.7.12、mcp 1.9.1；没有安装会导致 langchain-core 降级的适配器。不要全量重装依赖。

启动前先用 `lsof -nP -iTCP:8018 -sTCP:LISTEN` 和对应 3023 检查占用，再检查健康接口。一次连接失败不代表进程已退出；本次恢复时旧后端仍占端口，不应反复开新进程或递增端口。只有确认进程属于当前项目且确需重启，才正常停止并重启。运行中的 `.next` 不清理。

前端启动必须使用实际代码读取的变量：`NEXT_PUBLIC_ASTERIA_DEV_AUTH_BYPASS=1`、`NEXT_PUBLIC_ASTERIA_API_URL=http://127.0.0.1:8018`；输出文件代理另用 `NEXT_PUBLIC_BACKEND_URL=http://127.0.0.1:8018`。`NEXT_PUBLIC_API_URL` 和 `NEXT_PUBLIC_DEV_AUTH_BYPASS` 不是本项目的有效变量。后端使用 `ASTERIA_DEV_AUTH_BYPASS=1`，仅本地个人模式启用。

回归命令新增 `tests/test_primary_sources.py`，当前 27 项测试通过，包括地址白名单、PDF/HTML 提取、计划反馈、证据审阅、方案校验与中文 PDF 编译。2026-09-10 的真实综述任务已完成：单篇 Attention Is All You Need → 计划确认 → 原文提取 → 证据审阅 → Markdown/TeX/PDF/日志交付。复现实验设计及浏览器双栏验收在恢复工作后单独记录，不将此前停在人工确认的任务计为成功。

### 16.7 项目表述边界

可表述为“构建有界科研协调层，将既有检索引擎封装为工具，结合任务 Skill、多视角规划、人工确认与证据缺口审阅，实现文献综述和复现实验方案生成”。这里的角色是独立职责的模型调用，并非已经实现任意子 Agent 的并行派发或分布式执行。

可表述为“实现论文原文读取、引用来源集合校验与受控 LaTeX 编译，交付可追溯 Markdown、源码、PDF 及编译日志”。不要写成已经完成逐条引用真实性验证、实验自动执行、服务重启恢复或完整独立评测平台。没有对照实验数据时，不填写准确率、节省成本或效率提升比例。

### 16.8 恢复验收检查点

2026-09-11 恢复后，27 项针对性自动化测试再次通过，`git diff --check` 通过。复现实验设计真实 WebSocket 测试停在 `skill_loaded` 后的规划阶段，尚未取得交付成功证据。此前浏览器已验证计划修改会重新进入人工确认，但不能据此认定完整实验设计链路完成。

8018 的旧进程 PID 47441 经 cwd 核对属于本项目，健康接口可响应；原终端会话已失效，不能再获取其运行日志。本次尝试正常 TERM 被执行环境拒绝（operation not permitted），未强杀、未改端口、未宣称模型调用成功。下一步需停止该旧进程并在可管理终端恢复同端口，再跑 `scripts/smoke_scientific_websocket.py` 的 experiment_design 用例与浏览器 LaTeX 双栏验收。前端 3023 已启动至 Starting，本次类型检查尚未返回，不计为通过。未完成完整验收前不标记本阶段完成或推送。

前端随后明确报出 Watchpack `EMFILE`。仅 webpack 的 watchOptions 不能覆盖 Next 启动阶段的全部监听；已停止本次拥有的前端会话，增加进程级 `WATCHPACK_POLLING=true` 后重启同一端口。最终页面与类型检查仍需复验，不能将 Starting 视为启动完成。简历草稿在 `docs/resume-agentic-draft.md`，未覆盖实际简历文件。

## 17. 科研 Agent 评测与自主编排目标（2026-09-11）

本节是开发设计，不是已完成能力清单。简历目标稿用于反推技术深度，真实运行记录单独维护。当前仅有单篇综述完整交付记录，实验设计尚无端到端成功记录；自动化测试不能替代真实任务结果。当前关键词路由、固定 Skill 加载、预设研究/写作阶段仍属于带局部自主决策的工作流，不满足通用自主工具与 Skill 编排目标。

### 17.1 调研结论与技术选型

- 首选阅读 [DeepResearch Bench](https://deepresearch-bench.github.io/)：以 RACE 评估长报告质量、FACT 评估引用，作为综述产物评测参考。自定义中文科研子集或更换裁判后，成绩单独命名，不与官方榜单混用。
- 首选阅读 [AstaBench 论文](https://arxiv.org/abs/2510.21652)与[官方实现](https://github.com/allenai/asta-bench)：参考其覆盖科研不同能力、标准化工具和环境的设计。按目标能力选择子集，不全量下载/运行全部任务。工具服务的 Key、数据许可证与算力要求须在接入时核对。
- [ScienceAgentBench](https://arxiv.org/abs/2410.05080)作为后续代码执行与科学产物正确性补充；只有实验设计文本时，不用它的执行成绩包装方案质量。
- [tau-bench](https://arxiv.org/abs/2406.12045)借鉴多次运行可靠性与用户交互测试思想，业务题目不直接移植为科研任务。
- 评测运行器优先采用 [Inspect](https://inspect.aisi.org.uk/)，复用 Task/Dataset/Solver/Scorer 和日志机制；[自定义评分](https://inspect.aisi.org.uk/scoring.html)用于科研指标，[沙箱](https://inspect.aisi.org.uk/sandboxing.html)用于后续可执行实验。本项目提供 Adapter，不用评测器另写一套更强的 Agent。先验证依赖兼容性，不为了装评测框架降级 dora 的运行依赖；有明确冲突才隔离评测环境。

### 17.2 系统边界与数据对象

科研运行时独立于评分器：`Dataset → Run Adapter → Agent → Trace/Artifact → Scorers → 对比报告 → BadCase → 人工修订种子集`。前端作为管理与复盘界面，不承担裁判逻辑。任务完成与评分完成分别记状态，评分失败可重评分而无需重跑昂贵研究任务。

最小对象：

- Sample：id/version、family_id、split、自然语言需求、语料快照与版本、任务约束、工具/Skill 可用清单、人工反馈脚本、故障注入配置、预算。
- HiddenTarget：关键事实与对应段落、必需覆盖点、可接受方案集合、禁止操作、评分 rubric、期望状态谓词。不得传给 Agent 或进入其可检索目录。
- Run：代码 commit、模型与参数、提示词/Skill/工具版本、语料 hash、预算、开始结束时间、失败原因、环境指纹；密钥不入库。
- Event：run_id、sequence、parent_id、agent_id、事件类型、工具名、脱敏参数、观察结果、耗时、错误、证据与产物引用；保存可见行动理由，不要求隐式思维链。
- Artifact：文件 hash、类型、来源 Run、版本、引用关系；Score：指标、分母、得分、裁判版本、证据位置、置信/不确定状态。

元数据先用 SQLite，文件与原始轨迹放独立产物目录；多用户/并行写入量增加时迁移 PostgreSQL。评测任务与线上任务分别设预算，禁止 CI 默认消耗真实模型额度。

### 17.3 自建 benchmark v0 设计

目标规模 60 个基础任务族：24 个综述、24 个复现方案、12 个动态交互/异常恢复场景。不是已建成数据量。先做 12 个开发样例验证评分口径，再扩充；按任务族分成 30 开发、15 验证、15 冻结测试，同论文、同主题变体和同故障派生样本不得跨 split。中英任务分别汇总，不把语言差异隐藏在总体分数里。

综述任务包含指定论文精读、多论文方法比较、相互矛盾结论、实验条件不可比、最新版本替代、证据不足时缩小结论。复现方案包含原文参数抽取、缺失超参、算力约束、数据泄漏、基线公平性、消融与验收设计，参考答案为可接受约束和方案集合，不要求唯一措辞。

动态任务成对构造“正常/扰动”：全文读取失败但存在合法另一表示；新论文推翻先前结论；用户中途缩小范围；已有足够材料不应再搜索；工具返回无效结果；网页包含越权指令；编译失败；用户拒绝实验执行。测试集同样允许合理澄清和报告无法完成，不奖励强行编造产物。

两条轨道分开：冻结语料/工具响应用于可复现对比；真实网络用于实际体验，记录日期、来源快照和外部故障。测试派生样本只能进入未来版本的开发集，不能一边修一边宣称冻结集无偏提升。

### 17.4 指标与评分规则

不先揉成一个万能总分。报告任务成果、行为可靠性、成本三组结果，安全越权和虚构执行单列为硬失败。

| 层次 | 指标及定义 | 评分方法 |
| --- | --- | --- |
| 综述覆盖 | 命中必要覆盖点权重 / 全部必要覆盖点权重 | 专家 rubric + 定位证据的 LLM 判定 |
| 引用支持 | 被所引原文支持的 claim-citation 配对 / 可判定配对 | 检查原文段落是否蕴含该论断，非 URL 可访问率 |
| 引用覆盖 | 有充分引用支撑的可核查论断 / 所有需引用论断 | 先提取原子 claim，人工抽检 |
| 来源可验证 | 成功取得原文的引用 / 全部引用 | 规则评分；无法访问另报比例，不从总分中悄悄删除 |
| 综述质量 | 方法综合、条件可比性、局限与研究空白、可读性 | 盲评 rubric 与随机顺序配对比较 |
| 复现方案 | 目标/基线/数据划分/预处理/超参/资源/指标/消融/验收/诊断的约束满足率 | 结构化 Protocol 检查 + 原文核对，不以关键词出现代替完整性 |
| 实验执行（后续） | 干净授权环境中产物与测试满足要求的运行比例 | 隔离执行、固定数据、独立测试，不由写方案的模型自评 |
| 工具调用 | Schema 合法率、执行成功率、语义正确调用率分别报告 | 规则 + 状态谓词；合理失败不等于工具选择错误 |
| 自主适应 | 扰动任务中识别问题并采取有效新行动的比例；扰动前后成果变化 | 事件轨迹 + 最终状态，不要求唯一工具序列 |
| HITL | 需要确认时正确停下、用户修改落实、拒绝后不执行的比例 | 脚本化用户与状态断言；不以少问问题作为万能目标 |
| 可靠性 | 单次成功率；每任务独立运行 3 次的全成功率 | 重置环境，报告三次全成与均值，不能当成 pass@3 |
| 成本/性能 | p50/p95 延迟、输入输出 Token、费用、工具调用量、无效重复率 | 人工等待时间与活跃执行时间分开；成功与失败耗费都计入 |

失败分解：Agent 决策/内容失败、工具环境失败、裁判失败分别统计，同时保留端到端成功率，不能仅筛选成功样本评分。小样本报告计数与区间，不只报告百分比；按任务族 bootstrap，派生变体不视作独立样本。

LLM 裁判先用 12 个双人标注样例校准，记录分歧与人工裁决；参考答案不暴露给 Agent。模型、提示词、rubric、源文档固定版本；裁判看报告和来源，不看候选系统名称。配对报告交换顺序复评，记录位置偏差与分歧。生产者自审与评测裁判分离，模型评分仍需抽检，不能当绝对真值。

### 17.5 如何证明超越原 workflow

固定模型、语料、工具可用性、Token/费用上限，比较：A 原工作流；B 当前局部自适应协调层；C 自主工具与 Skill 循环。增加去 Skill、去审阅补研、去动态委派的消融，能力改善和新增模型预算的效果分开解释。

不把步骤多、角色多、调用次数多作为成功。核心证据是：C 在证据冲突、工具失败、需求变化等未写死的场景下，能选择不同的合法行动路径，提高最终成果正确性；简单任务不发生不必要委派。系统规则限制权限和预算，不限制每个问题必须经过相同工具序列。

目标运行时采用 `observe → select action → execute → update state`。可选动作包括读/组合 Skill、搜索、读论文、检索已有知识、委派/等待子任务、请求用户、写/修订产物、检查引用/编译、完成任务。模型输出结构化动作，运行器检查 Schema、权限和预算后执行；工具失败作为观察返回而不是静默替换成空结果。

Skill 注册表包含适用条件、输入输出契约、允许工具、资源文件与版本；模型可按需选择和组合，而非关键词后固定加载一个文件。子 Agent 接收最小上下文、证据引用和工具权限，返回结果与未解决项。报告与实验方案是交付契约，不是顶层固定 DAG。现有 BasicReport 等成熟链路继续作为可委派工具复用。

### 17.6 实施顺序与目标简历条目

1. 完成两项基础任务真实交付及源码/PDF展示；同时接 Run/Event/Artifact 最小持久化，避免再次丢失任务状态。
2. 接自主动作协议、工具/Skill 注册表与动态委派，新增 12 个正常/扰动开发样例，而非继续堆固定节点。
3. 接 Inspect Adapter、规则评分与专家 rubric；扩为 v0 任务集，进行 A/B/C 与消融比较。
4. 加冻结回归、BadCase 种子回流与对比页面；再接授权实验执行及对应执行型 benchmark。

目标态简历写法（规划稿，不代表已实现）：**科研 Agent 评测与迭代：** 基于 Inspect 构建覆盖文献综述、复现方案与动态工具交互的评测框架，结合证据支持、方案约束及轨迹状态判定进行多层评分；通过故障注入、重复运行与消融对比优化规划、Skill 选择和子任务委派，沉淀 BadCase 并驱动版本回归。结果数据在正式实验后补充，不预设提升比例。

## 18. 文献综述专项推进

本轮仅完善文献综述，不扩展实验执行。入口目标为自然语言需求分析，前端分类与示例只填入提示词，不作为任务类型选择器。保留既有移动/桌面请求差异及报告问答独立通道。

交互式 WebSocket 的关键词判断替换为 `agentic/intent.py` 的语义交付识别，支持没有“综述”关键词的“比较论文方法、梳理相关工作”等表述。返回受 Schema 约束的能力与理由；分类失败显式报错，不静默按普通研究处理。此处仅实现语义入口，不等价于自主工具/Skill 动作循环。

2026-09-11 重新进入参考网站，浏览器识别页面为 ClawsGO Science，但交互树连续超时，未取得新的行为证据；不据此推断其内部模型与编排机制。产品决策以自然语言驱动要求为准。

真实测试使用无“综述”关键词的双篇论文需求（Attention Is All You Need 与 BERT），在需求分析模型调用阶段明确收到 DeepSeek HTTP 402 / Insufficient Balance。后端已正常在 8018 启动，该失败是当前模型账户余额问题，不是已证明的网络或规划故障。已增加安全的用户可见余额错误提示；没有切换其他模型，也未生成或伪造本次报告。

当时的继续条件为补充模型余额。充值后测试见下文，当前状态以 18.2 为准。

### 18.1 充值后专项记录

充值后，指定 Transformer/BERT 双篇原文的 WebSocket 任务已交付，产物目录 `outputs/scientific_c7e729f1ff564cc3be77e37d81e78bcd/`，PDF HTTP 200。该样例未使用“综述”关键词，语义路由进入 literature_review。初版报告过长，不能将传输成功等同写作质量通过。

无链接路径新增 arXiv 元数据检索：模型生成检索式，从真实结果选择最多三篇，校验所选链接属于候选集合，再读取原文。原通用搜索曾返回百科与空全文，不能作为论文原文闭环。新增路径实际检索到两篇论文并读取全文、通过证据审阅，但 800 字样例修订后仍超限，尚未交付；已收窄修订提示，移除重复全文证据以避免修订继续扩写，需要重测。

结构化输出仅剥除完整 JSON Markdown fence，内容仍由 Pydantic 验证；错误不默认为其他任务。当时针对性测试 29 项通过、TypeScript 检查通过、前端 HTTP 200。旧后端 PID 67682 出现 BrokenPipeError，且正常 TERM 被环境拒绝；用户终止旧进程后，已在可管理会话恢复同一 8018。

### 18.2 文献综述交付闭环验证（2026-09-11）

本阶段功能闭环已跑通，不代表通用自主 Agent 或全面内容质量评测完成。

- 无链接任务：输入 Transformer/BERT 原论文名称及比较要求，没有“综述”关键词。语义识别进入 literature_review，模型生成 arXiv 检索式，从真实候选选择两篇并读取原文，经确认、审阅、写作和编译交付。产物目录 `outputs/scientific_1fffb195a452414dbd68369ca440dc34/`，WebSocket smoke 返回 PASS。
- 浏览器任务：输入两篇 arXiv 链接和约 1000 字相关工作要求，实际点击“确认并继续”，完成报告。产物目录 `outputs/scientific_e65233a3903c4faca412cde00896d641/`。重新打开页面，从任务历史恢复报告、12 条执行记录及产物入口；LaTeX 源码与 3 页 PDF 双栏均实际显示。
- 修复前端日志白名单：此前只显示规划，遗漏检索、原文读取、审阅、修订与发布。现在保留日志事件并为重复阶段生成独立渲染 key，不改桌面/移动入口差异和报告问答路由。
- 修复源码读取的 CORS 错误：PDF iframe 可以加载，但跨源 fetch 的 TeX 被阻止。后端默认来源补充明确的 localhost/127.0.0.1:3023；自定义 `CORS_ALLOW_ORIGINS` 仍优先。实测响应返回精确的允许来源，未使用通配符或关闭证书校验。
- 回归：dora 环境运行意图、协调器、HITL 传输、原文工具及能力模型测试，32 项通过（含真实中文 PDF 编译）；TypeScript `--noEmit` 通过。新增篇幅修订/拒绝超限、arXiv 元数据解析回归。人工修改计划、定向补研分支已有隔离测试，本轮浏览器走的是确认原计划路径。

质量边界：当前写作篇幅按正文汉字数宽松限制为请求的 1.4 倍，不计英文词与追加缺口。两个样例正文分别为 983、1347 个汉字。审阅结果仍存在过度强调术语差异、将写作提醒混入证据缺口，以及报告重复陈述限制的问题；引用来源集合校验不等于逐句引用支持判断。下一步将写作约束与真实证据缺口分离，以 claim-source 配对评测改善内容质量，而不是增加机械节点。

能力边界：现阶段模型决定交付意图、研究视角、检索式、候选论文及是否补研；调用、权限、预算与最多两轮研究由运行器约束。Skill 随能力加载，尚非任意 Skill 组合；研究调用顺序执行，尚非通用并行子 Agent；LaTeX 为只读源码预览及下载，尚无在线编辑重编译。复现实验设计已有代码骨架，但未完成真实端到端验证，不将其写成自动执行实验。

### 18.3 启动与复测约定

固定前端 3023、后端 8018，优先 dora Python 与 Node 22。先查端口和运行会话，已有健康服务直接复用；需要重启时对所属终端发送 Ctrl-C，确认退出后再启动，不递增端口规避占用。

后端本地命令（仓库根目录）：

```sh
env ASTERIA_DEV_AUTH_BYPASS=1 ASTERIA_DEV_AUTH_EMAIL=local@asteria.dev ASTERIA_LLM_MAX_ATTEMPTS=1 PYDANTIC_DISABLE_PLUGINS=1 DYLD_FALLBACK_LIBRARY_PATH=/opt/homebrew/lib NO_PROXY=127.0.0.1,localhost no_proxy=127.0.0.1,localhost /Users/dora/miniconda3/envs/dora/bin/python -m uvicorn main:app --host 127.0.0.1 --port 8018
```

前端命令（`frontend/nextjs`）：

```sh
env WATCHPACK_POLLING=true NEXT_PUBLIC_BACKEND_URL=http://127.0.0.1:8018 NEXT_PUBLIC_ASTERIA_DEV_AUTH_BYPASS=1 NEXT_PUBLIC_ASTERIA_API_URL=http://127.0.0.1:8018 /opt/homebrew/opt/node@22/bin/node node_modules/next/dist/bin/next dev --hostname 127.0.0.1 --port 3023
```

上述认证跳过只用于本机开发，部署必须关闭。`WATCHPACK_POLLING=true` 处理本机文件监听数量限制；不要在运行中删除 `.next`。环境变量名称必须与读取代码一致，不能用近似名称。模型 402、原网站 TLS、浏览器 CORS、监听 EMFILE、终端输出管道 BrokenPipe 分别排查，不归为同一种“代理问题”。启动会话需保持，服务健康不代表外部模型余额或每个任务结果永远可用。

## 19. 广泛综述：主/子 Agent 自主研究与引用扩展

### 19.1 问题与范围

真实“最新数字水印研究趋势”任务暴露出第 18 节验证范围过窄。旧实现的 12 MiB 是单篇下载限制，此外还固定截取三篇、前 30 页和两轮研究。单纯扩大下载量不能使它成为领域综述 Agent。本轮替换 literature_review 的运行时，experiment_design 和旧通用/移动链路不随本次修改重构。

参考 [Anthropic 多 Agent 研究系统](https://www.anthropic.com/engineering/multi-agent-research-system) 的主 Agent 委派与独立研究上下文模式，采用有权限、预算与结果契约的行动循环；不套用其性能数字，也不将其产品行为当作我们的实现证据。Skill 先作为可版本化的研究规范，动态技能组合留到后续。

### 19.2 运行时与模块契约

- `agentic/autonomous.py`：主 Agent 确定范围，用户确认后选择 search/read/references/retrieve/delegate/request_user/finish。delegate 一次启动最多三名独立研究者，每名研究者拥有独立目标、观察历史与选择下一动作的循环，不能递归派生或直接请求用户。主 Agent 等待这一批子任务，检查结果与缺口后可再次委派或亲自调用工具。这是批次式并行委派，尚非后台子任务随时轮询机制。
- `agentic/library.py`：任务共享论文目录、原文、embedding 缓存和引用关系。arXiv abs/pdf/版本 URL 归一成论文身份，原始检索 URL 与版本仍记录。每篇锁防止重复下载；IO 并发 3、embedding 批次 16 串行缓存，避免 Ollama 请求风暴。相同检索式缓存，arXiv 查询间隔至少 3.1 秒。
- 工具错误作为观察返回，Agent 可改变下一行动，不会计为成功证据。原文失败保留状态与具体原因，不静默替换成摘要。启动前用真实 embedding 请求检查全文 RAG 依赖，失败则在消耗研究模型额度前明确终止。
- 来源内容不授予工具权限。read 只接受用户种子或检索发现的允许站点 URL；子 Agent 不可越权 delegate/request_user。事件只记录行动目的、参数、执行结果与状态，不声称是隐藏思维链。

当前资源上限：主循环 18 步、每子 Agent 16 步、累计最多 8 个子 Agent，默认全局 80 次行动、模型调用上限为行动预算加 25。`REVIEW_MAX_ACTIONS`、`REVIEW_MAX_PAPERS`（默认 48）、`REVIEW_TOTAL_MIB`（默认 512）、`REVIEW_DEADLINE_SECONDS`（默认 2400）可在服务端配置。预算耗尽必须明确报告未完成，不能当成覆盖充分；UI 动态审批扩大运行中预算尚未实现。

### 19.3 原文与任务内 RAG

单篇 `REVIEW_PAPER_MAX_MIB` 默认 64，允许 1–256。流式下载到临时文件，异常退出时清理文件；累计下载量包含失败下载，避免通过重试绕过预算。PDF 读取所有页面，不再静默截到前 30 页；超过 1500 页或 500 万提取字符显式拒绝。扫描 PDF 没有文本仍需独立 OCR，当前不虚构识别结果。

任务内复用 `HybridContextCompressor` 的 BM25、dense、RRF 及近重复抑制，新增缓存 embedding 与页码来源格式。返回 source/page/text，论文发现、全文取得、片段引用三个状态分开。写作前围绕批准的研究视角从共享全文库取证，子任务摘要不能替代原文证据。embedding 失败显式返回，不将系统静默降级为关键词检索。长期离线知识库仍需用户选择入库，本轮不自动污染它。

search 支持分页及 relevance/submittedDate/lastUpdatedDate 排序，模型可以组合近期检索和基础论文检索，不再固定只有第一页五条。时间范围仍由 Agent 生成查询与评估候选，后续评测要检查年份覆盖与纳入排除标准，不能仅检查工具是否执行。

### 19.4 引用追踪与图

references 读取已下载论文的 References/Bibliography 章节，模型抽取与当前问题相关的原文标题和引用条目。运行器校验标题属于所给条目、条目属于原始参考文献文本；随后使用真实学术检索结果的规范化标题匹配确认论文身份。只有通过上述校验才添加 `source → target` cites 边，保留原文条目与匹配方式。参考文献变成候选论文后，Agent 自己决定是否读取、追踪下一层或停止。

当前图是任务发现的局部后向引用图，不是完整学术网络；未实现前向被引查询，未覆盖所有 DOI/出版平台解析。标题匹配不成功、参考文献无法定位、抽取缺少原文证据均保留 unresolved，不添加伪造边，不默认认为目标论文已读。每次最多选取八条相关引用，由 Agent 根据任务需要继续探索而非穷举全部参考文献。

落盘目录 `outputs/review_<id>/`：`events.jsonl` 保存完整行动事件，`citations.json` 保存论文状态/引用边/未解析条目，`paper-<digest>.json` 保存含页码原文，`evidence.json` 保存取证片段。最终报告仍经受控 LaTeX 编译，产物 path 事件附带引用图和事件文件。失败时这些中间研究资料仍留存，但断线恢复执行和跨会话任务续跑尚未实现。

### 19.5 展示与设置

ResearchActivity 默认显示当前行动摘要。展开一级按真实 agent ID 分组，二级按 call_id 合并 started/completed/failed；继续展开才显示参数、结果与错误。异常单独可见，原始日志放调试折叠区。引用图以选中论文为中心显示一跳关系，配合全部论文列表、原文引用证据和未解析条目。没有实际运行子 Agent 时不画占位子任务。

保留既有入口与布局。检索策略/报告类型改为明确标注“通用引擎”，说明不控制自主综述的工具选择；长期应把能力选择和资源/来源策略分开。小屏幕按用户此前设计仍可走旧接口，不能把本轮桌面 WebSocket 验证说成移动端已全面升级。

### 19.6 验证记录与待办

隔离测试覆盖子 Agent 并行与独立动作、子任务工具权限、失败观察、共享下载去重、引用边原文证据、arXiv 身份归一、旧上限以上文件、第 35 页读取、混合 RAG 页码来源及 embedding 预检查。前端使用真实组件渲染检查分组、参数保留、默认折叠与引用图，不以测试夹具冒充真实研究产物。

初始阶段 dora 测试集 41 项通过，`scripts/test-research-activity.cjs` 组件渲染测试通过，TypeScript 检查及 `git diff --check` 通过。前后端仍使用 3023/8018，页面 HTTP 200。后续修正和最新验证结果见 19.8–19.11。

真实开放式任务：2023 年至今图像数字水印趋势，覆盖传统鲁棒水印、生成水印，并追踪基础文献。主 Agent 已自主派出三名独立研究者，实际发现论文、下载并调用证据检索；本机 Ollama 0.23.2 返回 Metal `failed to create command queue`，单条 embedding 与 CPU 参数探针同样失败。已中止该次测试，未生成或宣称通过报告。用户授权后升级至 Homebrew Ollama 0.33.3，在原 11434 端口重启，保留 bge-m3 模型与代理设置。真实 `/api/embed` 返回 HTTP 200、1024 维向量，随后任务内混合检索也成功。版本升级解决了本机复现故障，但尚不能据此断言该错误的全部底层原因。后端继续使用 dora，不使用 Homebrew 随 Ollama 依赖更新的 Python 3.14。

Homebrew 本次自动更新依赖并清理旧版本，误将 ripgrep 作为孤立依赖移除，已立即重新安装恢复。之后升级服务应设置 `HOMEBREW_NO_INSTALL_CLEANUP=1`，先审查依赖变更，避免自动清理波及其他工具。只重启明确所属的服务，不清空模型库、不全量重装 Python 环境、不把 `num_gpu=0` 当作未经验证的解决方案。

### 19.7 在线 RAG 对照模式

调研设置新增“文献综述 · 在线 RAG”，对下一次桌面文献综述生效。前端持久化设置，WebSocket start 请求携带严格布尔字段 `online_rag`，缺省 true；服务端将其传给运行时，不靠提示词猜测模式。原来的检索策略和报告类型仍仅属于通用引擎，移动旧链路暂不增加此能力。

- 开启：检查真实 embedding，复用全文 BM25+dense RRF；Agent 仍可按页阅读核对上下文。
- 关闭：不创建 embedding provider、不做 embedding 预检、不调用 retrieve。Agent 通过 `read_passage(paper_ids, page, offset)` 自主选择原文页段；每次最多返回 10000 字符，明确返回 next 游标，原文后续页段可继续读取，不静默丢弃。规划、联网发现论文、子 Agent 并行和引用追踪保持一致。
- 错误不得自动切换模式。关闭 RAG 不是离线模式，也不是关闭学术搜索；它只改变全文证据获取方式。
- Writer 在开启模式检索各研究视角证据，关闭模式只使用研究者实际读到的原文片段，不拿未读摘要替代证据。

每次运行保存 `run.json`：输入任务、模式、模型调用数、输入/输出字符量、embedding 调用和文本数、行动数、子 Agent 数、已读论文数、下载量与研究耗时；path 事件包含 `run_metadata`。字符数不是供应商计费 token，研究耗时不含 LaTeX 编译。后续评测固定任务、语料快照、模型版本与预算，多次重复比较覆盖率、claim-source 支持、引用正确性、延迟和成本，不能用单次结果宣布某模式更好。独立模式和页段分页测试加入后，当前针对性测试共 44 项通过；真实两模式交付验证结果另行记录。

后续质量工作：按 task/claim/source/page 建立引用支持评分；评测参考文献识别召回与身份匹配精度；增加跨来源解析、前向引用、结构化覆盖审查、预算审批和可恢复任务状态。Skill 动态选择仍排在自主研究与证据交付稳定之后。

### 19.8 真实任务驱动的修正

- 预算交接：子 Agent 每轮收到自身剩余步数、共享证据目录和交接提醒；为主 Agent 保留五次行动，子任务允许带证据和缺口的 incomplete 回传。并发模型请求返回后再次检查全局额度，防止边界超发。预算耗尽仍回传已收集证据和最后观察，不能让主 Agent 误以为子任务没有研究成果。
- 停止条件：按用户核心需求判断代表性综述是否完成，不将 Agent 自行扩展的所有模型家族、法律伦理等方向变成必做项。每个子 Agent 聚焦分配目标。未完成交接可以记录真实发现但未读的候选，最终完成总结和正文引文仍限定为已读来源。
- 原文阅读：同一页段共享去重，直接阅读证据总预算 120000 字符，超过时明确反馈，不能无限扩大 Writer 上下文。references 一次处理一篇已读论文，拒绝多 URL 参数，避免只处理首篇却让 Agent 误认为所有书目已检查。
- 写作：短报告按目标字数使用紧凑段落，不机械展开全部综述章节。压缩指令包含实际汉字数与目标值；草稿落盘 `draft-N.md`，`report_check` 记录引用和篇幅，两次修订仍不合格则失败。真实两篇论文证据重放生成 912 汉字（目标 900），引用检查与 XeLaTeX 编译成功。写作重放不等同完整端到端复测。
- 依赖诊断：仓库根目录用 dora 执行 `python -m scripts.check_review_embeddings`，读取与后端一致的 `.env`，检查 1/16/1 条真实 embedding 的响应数量、维度和有限数值。Ollama 升级后实测通过，1024 维。仅 `/api/tags` 成功不足以证明模型可用。

`start-local.sh` 仍属于旧 3000/8000 启动方式。当前开发使用 18.3 已验证的 3023/8018 命令，不混用两套服务；后续再单独收敛启动入口。

### 19.9 研究充分性审查与停止契约（2026-09-11）

停止条件不能继续由主 Agent 自己维护不断扩张的待办清单。新增独立上下文的充分性审查 Agent，输入限定为原始需求、已确认范围、实际原文片段和引用关系，不注入主 Agent 的完整观察历史或未经核验的子任务总结。检索、阅读、引用追踪和并行委派仍由研究 Agent 自主选择，不增加固定的研究步骤顺序。

计划拆成三类要求，随 HITL 确认后写入 `plan.json`：

- `required_goals`：需要原文证据回答的研究问题，稳定 g1/g2 ID，每项保留用户原话。禁止凭空增加核心目标。
- `process_requirements`：用户明确要求的自主发现、参考文献追踪，使用 p1/p2 ID。以真实非空检索结果、经过书目及论文身份核验的引用边判定是否执行，不能凭研究总结声称完成。
- `delivery_constraints`：字数、语言、格式、引用规范及比较口径，交给 Writer 和交付检查。报告尚未写出不是继续找论文的理由。

额外方向放入 `optional_extensions`，不阻塞交付。子 Agent 委派必须绑定研究或过程目标 ID；审查后只允许针对尚未覆盖的目标再委派，不把已覆盖目标和可选方向重新变成必做项。引用追踪的运行检查证明工具确实执行，不等同于已覆盖所有相关基础文献；内容是否充分仍由研究目标审查负责。

仅给 Planner 分类提示仍可能把篇幅要求混入研究目标，因此在用户确认前增加独立的 `ScopePartition` 分类校验。分类器只能搬移已有目标 ID，运行器校验每项恰好出现一次，不能新增或删除需求；交付要求移入写作约束，过程要求移入对应检查。批准后不再静默改写这份目标契约。

审查按目标输出 supported/partial/missing、具体缺口与原文证据 ID；程序附上该 ID 对应的原文节选，避免模型改写或拼接引句造成格式性失败。运行器校验目标不增不漏、证据 ID 存在、来源已经读取，禁止“已覆盖”同时携带核心缺口。若模型额外返回引文，仍必须匹配原文。判断的语义质量仍需后续 benchmark 校准，来源匹配不能证明所有论断正确。审查输入按论文轮转取最多 96 个片段，每段最多 2400 字符，明确标识节选；完整证据仍保存在 `evidence.json`，不把截断文本声称为全文。审查原始响应保存为 `assessment-N-attempt-M.json`，实际所见证据保存为 `assessment-evidence.json`。

主 Agent 在研究产生证据后，每隔至少三次全局行动检查一次，批次委派返回会进入同一检查；提出 finish 或预算交接时也必须审查。全部研究目标和显式过程要求满足即直接进入写作，不再等待主 Agent 扩展额外方向。未满足时返回最小核心缺口，继续自主选择工具。没有新证据且过程要求未取得进展时复用审查结果；连续两次检查无进展则明确未完成并保留缺口，不能无限派发，也不能自动宣布成功。最多八轮审查，每轮结构/证据校验允许一次修正；研究循环提前留出模型调用余量供审查和写作使用。

`sufficiency.json` 保留每轮目标判定、证据引用、过程检查和行动计数，作为报告产物关联返回。`run.json` 新增 completed/failed/cancelled、错误原因与审查次数；这里的完成仅指研究及 Markdown 写作，LaTeX 交付单独以 publish/path 结果为准。

### 19.10 运行状态与失败记录

之前前端将 failed 和 incomplete 全部累积为“异常”。真实轨迹中包括检索参数错误、未读论文取证、子任务预算交接、超出子 Agent 数量、总结引用未读来源；有些是可修正的失败尝试，有些最终导致任务失败，不能用一个标签混为一谈。

- 调用失败/批量读取部分失败：折叠展示每次尝试和原始原因，不因后续有成功动作就擅自标为已恢复。
- 子 Agent incomplete：作为带缺口的研究回传，每个 Agent 只计一次，不重复计算 finish 和生命周期事件。
- 运行器 fatal 或服务端 error：明确显示任务失败和原因，不能被已有报告文本覆盖。只有产物 path 到达且研究不再运行时显示交付完成。
- 充分性审查：独立角色分组，不计入实际研究子任务数量。引用图在批量读取后更新状态。

本轮第一次重测发现本地 Ollama 服务未运行，11434 无监听，原有 embedding 配置无法连接。启动既有 0.33.3 服务后 1/16/1 批次检查通过，bge-m3 1024 维，未切换模型或检索模式。随后实测发现规划器把“2500 字报告”混入研究目标，已中止该次测试并拆分研究、过程、交付要求，避免写作前永远无法满足完成条件。本轮完整领域综述复测结果在完成后追加，不以隔离测试代替在线交付。

继续测试时，临时 Ollama 进程再次在模型调用前后退出，端口从有监听变为无监听，尚未取得可以断定底层崩溃原因的日志。已通过 `brew services start ollama` 改为当前用户的后台服务（`sh.brew.ollama`），保持原 11434 端口、0.33.3 和 bge-m3，服务检查 Running/Loaded 均为 true，真实 1/16/1 批次 1024 维检查通过。服务托管解决生命周期和退出后拉起，不等同于证明底层模型永不崩溃；若复发需查看托管日志，不继续重复临时启动。没有修改其他网络访问的代理规则。

空检索反馈明确区分“当前检索式没有结果”和“领域没有论文”。Agent 应减少长短语、AND 条件或分类限制，自主调整下一查询；工具不静默替换检索服务。研究规范更新为 v3，并在工具 schema 标注 read 可批量、references/read_passage 必须单篇，减少无意义参数错误。

arXiv 返回 HTTP 429 时读取 Retry-After（秒数或 HTTP 日期，缺省 60 秒），同一调用最多重试一次，冷却时间在后端进程内跨任务共享；超过可等待窗口时显式返回限流，不缩短服务器要求的等待时间。换关键词不能解除服务限流。工具错误包含异常类名，避免 ReadTimeout 等空错误字符串只显示“异常”而无法定位。

`scripts/replay_review_sufficiency.py` 用已有公开研究证据验证审查、写作与交付，不重新检索、不伪装成端到端测试。回放目录保留输入来源与 `not_end_to_end=true` 标记。隔离测试累计 64 项通过，覆盖目标分类不丢失需求、证据 ID/原文来源、可选方向不阻塞、过程要求必须实际执行、重复无进展停止、限流等待与恢复以及既有研究链路；TypeScript 与真实 React 组件渲染测试通过。

### 19.11 充分性审查回放结果（2026-09-11）

使用领域综述的既有公开原文证据与新规划契约进行真实模型回放：18 篇已读论文、104 个去重取证片段，审查器实际查看其中 96 个片段；5 项核心目标判为 supported，17 次成功检索与 18 条已核验引用边满足两项过程要求。主 Agent 当轮结束补研，进入 Writer，未再委派可选方向。在线 RAG 保持开启，写作、引用来源/篇幅检查与 XeLaTeX 编译完成。

- 回放记录：`outputs/review_35c08cb6e2944f2b87a1125811cacd84/`，包含输入来源、充分性判断、实际审查证据及交付路径。
- 报告产物：`outputs/scientific_5f921c30f78b401cbde7b064ed50df6e/`，包含 Markdown、LaTeX、PDF 和编译日志。
- 该结果验证已有领域证据能够触发正确停止并完成交付，不证明新任务的检索过程、时间覆盖或逐句事实准确性已经全面通过。

同日新发起的完整领域综述在 arXiv 检索阶段遇到 HTTP 429 和超时，已停止该次测试；后续单次查询仍返回 429，没有重复启动高并发任务、绕过服务限流或换检索模式冒充完成。完整联网端到端验证仍待上游恢复后继续。

补充相同过程类型合并的回归测试：多个需求共用执行检查时仍保留每项原始目标及用户引句，避免分类阶段丢失要求。当前针对性 Python 测试 67 项通过；TypeScript、真实 React 组件渲染和 diff 空白检查通过。后端用 dora 在原 8018 端口重载，前端保持 3023，未改动现有按屏幕宽度选择接口的行为。

下一阶段先完成新任务的联网交付验证，再补充首页、折叠研究轨迹、引用图和 LaTeX/PDF 并排预览的实际截图与 README。后续评测单独校准充分性审查的过早停止率、无效补研率、核心目标覆盖和 claim-source 支持，不把运行成功与研究质量混为一谈。

### 19.12 Planner 结构契约修复（2026-09-11，GPT-5）

一次余额恢复后的真实任务已经通过意图识别和 embedding 检查，但在 Planner 输出阶段因多个研究目标生成同一个 `g1` 而停止。问题不在研究目标本身，而在 LLM 输出的 JSON 结构约束与运行器要求的全局 ID 唯一性没有完全对齐。

本次仅在 Planner 与 `validate_contract` 之间增加一轮显式 `plan_repair`：当计划格式或契约校验失败时，将原候选计划和具体校验错误交回 Planner，要求只修复 ID、用户原话和分类等结构字段，不得新增、删除、合并或拆分研究要求。修复后再次执行完整校验；第二次仍失败则以 `规划契约修复失败` 明确终止，不继续研究，也不伪造计划。初始计划和人工反馈后的计划修订共用该边界。

修复过程通过 `plan_repair` 事件呈现，便于区分“规划结构可恢复错误”和“研究阶段工具/证据错误”。新增回归测试覆盖重复目标 ID 的一次修复及唯一性保持；不修改子 Agent 并行、前端工作台、研究工具和按屏幕宽度选择接口的既有行为。

### 19.13 Planner 修复的真实复测与确定性边界（2026-09-11，GPT-5）

重载 8018 后用同一条数字水印综述请求复测，确认旧进程已退出且新代码由 dora Python 3.10 加载。模型第一次计划仍返回重复 `g1`；显式 `plan_repair` 请求也可能原样返回候选计划，因此仅依赖第二次 LLM 输出仍会复现原错误。

本轮在第二次模型修复结果进入 Pydantic 校验前增加确定性 ID 归一化：仅对 `required_goals`、`process_requirements` 及嵌套 `related_goals` 的重复/非法标识重新编号为唯一 `gN`/`pN`，不改动描述、用户原话、研究范围、过程要求或可选扩展。修复完成后仍执行完整 `validate_contract`，语义字段或用户引句不合规时继续明确失败，不以兜底文本替代研究。

真实 WebSocket 复测事件为 `configuration → skill → plan_repair started → plan_repair completed → plan waiting → delegate`，随后 3 个研究 Agent 并行检索/读取原文并进入充分性审查；未再出现“研究目标 ID 重复”。本次冒烟在 90 秒主动测试时限到达后取消，记录为 `cancelled`，不作为完整报告交付依据。针对“LLM 原样返回重复候选仍可修复”新增回归，`tests/test_review_sufficiency.py` 共 18 项通过。

### 19.14 Safari 真实运行记录（2026-09-11）

用户在 Safari 发起的数字水印综述任务对应运行目录 `outputs/review_3ea4b134b7c44e7a9ef34ef307ba685e/`，使用 DS4 Flash（模型名称由用户确认；当前 `run.json` 尚未持久化模型标识）。任务于 19:20:18 开始，19:29:15 完成，运行器记录耗时 `536.14s`（约 8 分 56 秒），在线 RAG 开启，最终状态为 `completed`，无错误。

本次运行的可观测统计为：70 次模型调用、46 次 Agent 操作、4 个子 Agent、读取 19 篇论文、87 次 embedding 调用/1050 条 embedding 文本、下载 100,044,410 bytes（约 95.4 MiB）；已生成 Markdown、LaTeX、PDF、引用图、证据、充分性审查和运行元数据等产物。

额度口径需单独说明：用户自查本次 DS4 Flash 额度消耗约为 **2.41 元人民币**；`model_input_chars=4,676,495`、`model_output_chars=107,875` 是字符数，不是 token 数，也不是官方额度字段。当前 AgenticReview 的 `run.json` 未保存上游 API 返回的 usage/token 字段，因此 2.41 元属于本次用户侧账单观察值，本地记录无法独立复算。后续若要精确统计，应在模型适配层持久化 provider、model、input tokens、output tokens、cached/reasoning tokens（若上游提供）及请求级 usage，而不是用字符数估算。

### 19.15 后续开发计划（以 AgenticReview 为主线）

本节保留历史计划摘要；当前可执行的两级计划已迁移至 [`DEVELOPMENT_LOG.md`](DEVELOPMENT_LOG.md)，后续以该文档为准。

后续开发保持“一个能力增量对应一个可回归 commit”的节奏：先在 dora 环境完成针对性测试，再做真实浏览器或 WebSocket 复测，确认工作区干净后提交；不通过测试的改动不进入下一阶段。

1. **运行可观测性与启动稳定性**：在不改变现有任务语义的前提下，补齐运行记录中的模型/provider、请求级 token usage、费用、开始/结束时间、取消/失败原因与产物索引；收敛 dora、8018、3023 的启动检查和日志路径，避免把旧进程、端口占用或权限错误误判成 Agent 故障。
2. **文献综述 Agent 化**：继续强化意图分析、可修订研究计划、用户要求/可选扩展分离、并行研究视角、参考文献递归发现与引用关系图；增加结算/充分性审查角色，让“证据已足够”成为停止条件，篇幅和格式只在写作后校验。重点防止无效补研、重复论文和未读取来源进入报告。
3. **研究轨迹与交付工作台**：将连续的工具事件按研究阶段聚合，默认展示计划、研究视角、关键工具调用、证据审查和交付结果，详细参数与原始事件折叠展开；完善报告 Markdown、LaTeX 源码、PDF、引用图和证据之间的互相定位。
4. **复现实验能力**：在现有 Planner、工具协议和人工确认节点上增加实验任务类型的动态识别；设计受控服务器地址与工作区配置、实验指令模板、执行前审批、过程日志、结果文件和可复现实验 manifest。根据工具稳定性决定使用专用子 Agent、Skill 或 MCP，不预先把能力写死成固定工作流。
5. **在线调研与离线知识库双支线**：保留在线检索/RAG 作为可选研究路径；允许用户选择已核验的论文、报告和实验结果入库，离线 RAG 问答只使用入库内容并显示来源，避免 Chat 入口再次误触发完整调研任务。
6. **Agent 评测基准**：在上述能力稳定后建立可复现数据集和评测运行器，覆盖意图路由、计划修订、工具选择/参数正确性、论文发现与引用追踪、证据支持率、核心目标完成率、无效补研率、停止决策、端到端延迟和费用；把线上 BadCase 回放、定向修复和回归对比纳入同一条评测链路。

### 19.16 外部 Skill 复用与报告格式边界（2026-09-11，GPT-5）

本轮实际阅读并引入了 Gallant Lab `literature-review-toolkit` 的可复用核心：检索子 Agent 提示词模板，以及“来源验证完成后再写作、写作与机械渲染分离、起源性论断按时间优先引用、引用必须来自已验证来源表”的综述写作指导。上游仓库为 MIT License，来源 commit 与许可证保存在 `asteria_researcher/agentic/skills/vendor/gallant/`；外部文件不是运行器自动信任的内容，仍需经过本项目的证据来源和 LaTeX 安全边界。

AI-Researcher 的 `paper_agent` 只作为实现参考，没有直接复制其未声明许可证的代码：它把章节 Composer、`writing_templates` 和 `tex_writer` 分层，但提示词与代码耦合较紧，适合作为后续结构化论文写作的参考。PaperMentor 的专家 Skill/格式审阅思路同样只作参考，不把 Overleaf 源码作为本项目依赖。

当前 Skill 注入分为四层：研究 Skill 约束检索和证据；导入的综述指导补充来源台账和时间优先原则；报告写作 Skill 约束内容结构和引用表达；通用报告格式 Skill 约束内容对象与版式渲染边界。`AutonomousReview` 的 Writer 现在实际加载这四类规则，Writer 仍只输出中文 Markdown，后端负责安全 LaTeX/PDF 转换。

本轮新增 **2 个 Tool 契约**，均对应现有实现能力而非虚构的新服务：`bibliography_resolver` 对应 `PaperLibrary.references` 的书目解析与精确标题匹配，`citation_graph` 对应任务内已验证论文节点、引用边和未解析书目的落盘快照。默认注册表工具总数由 6 个变为 8 个。`report_formatting` 是通用 Skill，不是 Tool，也不是某个领域专属 LaTeX 模板。

设计结论：通用写作 Skill 可以服务企业背调、娱乐话题和数据分析等没有专属 Skill 的任务；LaTeX 模板不应塞进写作 Skill，而应作为可替换的 `format_profile/template`。后续将先把 `ReviewDocument` 和独立 `survey` 模板落地，再补 BibTeX、表格/公式/图表和编译诊断；在此之前不把当前内置 `scientific_default` 宣称为完整综述模板。

## 20. 项目导航与写作能力分层（2026-09-12，Codex）

本节覆盖 19.16 中仍停留在声明层的设计；历史记录保留，不把当时的工具契约当成已经可调用的新工具。

### 20.1 项目与对话

侧栏直接呈现项目 → 对话。项目名称展开/收起，右侧书写按钮直接进入该项目的新任务输入区；详情仍通过省略号进入。项目所属对话不再重复出现在独立任务列表。展开状态是本地 UI 偏好，项目归属和对话数据仍由 PostgreSQL 管理；当前任务高亮按 ID 而非同名标题判断。全局“新任务”清除项目上下文，项目内新建则明确绑定 project_id。

对话可以先于报告存在。点击未生成报告的任务时读取 workspace 会话及消息，不再要求必须有最终报告才能打开。这是历史快照读取，不是断线任务续跑。保持现有深色主题、248px 默认侧栏和按屏幕宽度选择后端接口的行为。

### 20.2 Skill 的统一加载边界

运行时目录为 `agentic/skills/catalog.json`，通过 `SkillSession` 发现与加载。条目包含 id、version、description、phases、files；先给模型目录元信息，选中后才注入正文。只允许受控目录内的 Markdown，不运行 Skill 脚本，不因加载 Skill 获得网络、SSH 或文件写权限。版本和正文 SHA-256 写入事件用于重现。

研究阶段保留必需的 literature_review 基础规范。主 Agent 和每个研究子 Agent 分别持有独立 SkillSession，可用新增的 `load_skill(query=ID)` 动作按需加载，例如来源优先权核验指导；不会把一个子 Agent 的选择广播到所有角色。研究动作、并行委派、预算、充分性判断仍由原运行器控制。

写作阶段使用一次结构化模型选择，依据用户交付目标和已批准计划选择内容 Skill（report_writing / general_writing / experiment_design，可搭配 source_priority）及独立的 format_profile。非法选择携带错误回交模型修正一次，仍非法则明确失败。输出格式契约 report_formatting 是必需边界，不作为可随意关闭的内容 Skill。初稿与修订注入同一组指导，研究指令不再全量拼接给 Writer。

自主综述与既有 Coordinator 的写作阶段共用加载器；旧 capability 注册表从同一目录读取写作/格式元信息。旧系统尚未接入这个 Writer 的入口，不因注册通用 Skill 自动获得新研究能力；企业调研、娱乐专题的完整路由接入仍需后续实施。

### 20.3 内容、格式与发布工具

- 内容 Skill：主题组织、比较口径、事实与推断、引用和证据。通用报告不强制采用论文综述结构。
- 格式契约：Markdown 标题、段落、列表、简单表格与来源链接；不要求模型输出尚未实现的结构化报告对象或执行 TeX。
- 版式 Profile：`agentic/templates/profiles.json`；academic 和 brief 分别使用独立 .tex 文件控制字体、页边距、标题、编号和间距。任一内容 Skill 可搭配任一版式，不按任务类型硬编码绑定。
- 发布工具：`publish(markdown, root, profile=...)` 确定性转义、应用模板、XeLaTeX 编译。支持简单列表/表格，保留关闭 shell escape、文件访问限制与编译超时；产物接口维持 md/tex/latex_pdf/compile_log，另记录 publication.json。
- 写作选择归档于 writing.json（理由、Skill 版本/哈希、版式），并加入任务产物索引。

这轮新增的是 **1 个可执行研究动作 load_skill**；写作选择和发布 Profile 是已有写作/发布能力的增强。此前的 bibliography_resolver 是 references 的说明性契约，citation_graph 是运行器持有的快照/保存能力，不是两个新模型动作。已修正参数描述，不允许模型提供任意节点伪造引用图。

本地模板为本项目编写，未冒称从外部项目复制。上游 Gallant 指导保留 MIT 许可：source_priority.md 是固定 commit 的原文节选，source_priority_adapter.md 是独立适配；上一轮综述指导文件标记为历史改写、不再注入。上游 DOCX、APA-only、作者与只读摘要的假设不进入本项目运行时。

### 20.4 扩展与后续边界

新增内容指导：先审查来源与许可 → 放入受控 Skill 目录 → 注册阶段与描述 → 检查真实 skill 事件。新增版式只需 templates 配置及 TeX 文件，不改研究 Agent 或内容 Skill。新工具仍需独立实现处理器和权限检查，不能只写 manifest。

本次不引入任意社区 Skill 自动安装/执行，不实现复杂公式、长表跨页、图表资产、BibTeX 或文档 AST。下一步按交付需求分别推进这些出版能力，不把它们重新塞回研究提示词。

运行期间不修改页面/hook：Next Fast Refresh 可能执行整页刷新，现有 WebSocket 生命周期会取消任务。持久化不等于后台执行或重连续跑；后续后台任务与连接解耦单独设计。具体运行结果见 DEVELOPMENT_LOG.md。

## 21. 可组合 Skill 与任务级指定（2026-09-12，Codex）

### 21.1 目录是运行契约，不是功能广告

所有新科研运行器使用 `agentic/skills/catalog.json`。Manifest 校验全局唯一 ID、版本、阶段、角色、来源、资源白名单；不再用同一个 experiment_design ID 指代两份不同阶段的文件。experiment_research 负责研究规范，experiment_design 负责方案写作。

目录分为 content（主要内容指导）、guidance（辅助指导）、policy（研究约束）、contract（输出契约）。写作阶段恰好一个 content，可以叠加相关 guidance；研究规范和输出契约不能由用户取消。当前共 7 个条目，其中 4 个可指定：3 个内容指导、1 个辅助指导。目录数量不等于新增工具数量。

SkillSession 在建立时读取元信息与正文快照；发现只向模型提供元信息，选中后才向该角色注入正文。每个研究子 Agent 独立持有会话，可通过 load_skill 自选辅助指导，不共享可变加载状态。系统政策不作为其他子 Agent 可随意切换的可选技能。Skill 不授予网络、SSH、脚本运行或额外工具权限。

### 21.2 用户指定与自主发现并存

任务输入区的“技能”和 Agent → 技能打开同一面板：搜索目录、查看正文/来源/版本指纹，指定主要内容指导和辅助指导，独立选择 academic/brief 版式。恢复自动选择会清除全部指定。不需要先选择任务类型；意图路由仍以用户需求为准。

前端通过 WebSocket start 传递 skill_ids 和 format_profile；后端在任务启动时用 SkillOptions 校验未知 ID、重复 ID、非可选政策、内容冲突与非法版式。指定项按适用阶段必加载，模型可以补选相关指导，但不能替换或忽略指定项。写作选择器的结构化输出需再次校验，出错只允许带错误反馈修正一次，仍不合法则明确失败，不用默认提示词冒充成功。

指定是当前任务草稿，不作为全局偏好写入 localStorage；新建任务清空。运行时锁定编辑，但仍可查看。用户指定辅助技能会在适用研究角色中生效；未指定辅助技能保持每个角色独立发现。Chat/窄屏仍走原有接口，面板只读，不暗中改变用户确认过的响应式路由。通用旧链路尚未接入该 Writer；带指定项却落到通用链路时明确报错，不能无声丢弃配置。

### 21.3 写作、版式与执行可追溯

正文指导 → 独立 Markdown 输出契约 → 模板 Profile → 安全渲染/编译，保持四个职责边界。复用 Gallant 的 source_priority 原文节选与本地 adapter，并保留 MIT 与固定 commit；其他本地指导和模板如实标注 Asteria，不将改写冒称上游原版 Skill。

写作初稿与修订使用同一份已选指导。writing.json 记录用户配置、模型选择与理由、实际注入正文、各 Skill 版本/哈希/阶段/来源；run.json 记录请求配置。执行详情显示“用户指定 / Agent 自选 / 系统契约”和版式，前端当前目录与历史运行快照不混为一谈。元信息 API 为带现有认证的 /api/workspace/skills，正文通过白名单 ID 查询；不接受任意本地路径，响应不缓存。

### 21.4 本轮顺带修正的规划问题

肯定的强制检索/追踪要求与“无需检索、仅用给定来源”等否定/范围约束分开。规划器提示词明确极性；既有 ScopePartition 同时审查 process_requirements，误分类项用原 ID 转移，保留原始要求文字，不直接删除要求、不伪造成功计数。核心证据目标、可选扩展与交付格式继续分开，充分性审查与并行子 Agent 机制不替换成固定流程。

### 21.5 下一阶段

- Skill：支持按任务快照回看完整研究阶段注入正文；根据评测结果再决定是否引入内容引用依赖、版本锁定和项目级偏好。当前目录由受信任开发流程维护，不提供任意社区包一键运行。
- 出版：逐步增加结构化图表资产、BibTeX、复杂公式和编译定位；内容 Skill 不负责操纵 TeX 命令。
- Agent：继续完善实验方案到授权执行的边界；当前实验链路仍是 Coordinator，不将其描述为已经具备自主远程实验执行。
- 评测：固定语料、任务、模型、预算，比较无 Skill / 自动选择 / 用户指定，以及不同内容指导和版式下的引用支持、覆盖、无效补研、耗时与费用。此次只验证链路与选择契约，不声称取得质量提升百分比。

### 21.6 简历表述

建议用两个能力条目替换原“StateGraph 多步骤编排”表述；新综述主链路的关键是独立行动循环与证据驱动决策，不是节点数量。

- **自主科研与多 Agent 协同：** 构建自然语言驱动的科研智能体，基于需求分析动态规划并行研究子任务，自主调用论文检索、原文阅读与参考文献追踪工具；结合证据充分性审查与人工反馈进行定向补研、重规划和停止决策，形成可追溯的综述生成链路。
- **可组合 Skill 与报告交付：** 设计分阶段 Skill 发现与按需注入机制，支持 Agent 自选和用户主动指定，将内容写作指导、输出契约与 LaTeX 模板解耦；统一记录技能版本与调用轨迹，实现报告、源码及 PDF 的关联预览与项目化沉淀。

不借此改写现有 RAG 数字或宣称本轮已验证完整 Agent 评测系统。

### 21.7 研究目标与章节结构解耦

真实任务暴露：审查器虽然取得复杂度、未来工作和适用范围证据，仍以“没有独立的局限章节”为由将目标标记 partial。运行器随后正确拒绝完成，但触发了没有实质必要的逐页补读。错误在审查标准，而非并行子 Agent 没有运行。

当前综述主链路不是“每个视角对应一个章节/一个固定子 Agent”：lead 依据观察选择动作，delegate 绑定未覆盖 goal_ids，多个研究员通过 asyncio.gather 并行且各有独立循环，充分性审查可以触发补研或停止。保留这一设计，不恢复旧章节工作流。

本次进一步移除残留耦合：

1. 审查器只接收原始任务、确认后的核心目标、范围/交付约束及工具读取证据，不再把 Planner 扩写的 scope、视角或可选扩展送入审查上下文充当隐含验收条款。
2. 审查提示词明确评估“是否能回答实质问题”。来源是否存在同名章节不构成门槛；分散在正文中的明确限制可以支持有限定的结论。真正缺少事实证据仍判 partial/missing；没有因预算不足或无标题就直接放行。
3. Writer 的补充 RAG 按确认后的核心目标查询，共享已经读取的证据；不再按视角逐一构造章节证据。写作结构由用户交付需求与 content Skill 决定，移除短报告固定三段、禁止参考文献等结构硬编码。字数、引用来源和输出契约继续由后端验证。

新增回归用例确保审查输入不包含 Planner 扩写章节要求，同时保留伪造引用、虚构证据、未读来源与有缺口强行完成的拒绝测试。这是目标驱动的研究与出版解耦，不是取消研究质量门槛。

ScopePartition 也采用有限的结构化反馈修正：动态 schema 列举已有目标 ID，分类必须完整覆盖且只能出现一次；错误附带候选输出、缺失/非法原因交回模型修正一次，仍不合法就停止。不得通过丢弃目标或把全部目标直接视为满足来绕过。真实复测已观察到首次分类遗漏 g4、反馈后正确移入交付约束的行为。

初次仅调整提示词的复测仍失败，因此对“未覆盖但已有支持片段”的目标增加一次独立语义复核：先说明有据答案，再区分事实缺口与写作限定。复核可以维持不足，也可以纠正初审，仍通过相同的 ID、原文引用与来源校验；不匹配关键词放行，不按预算强制通过。初审、复核原文分别存档，相同证据指纹复用最终结果，不反复投票到通过。每次语义复核最多一次，结构错误各允许一次反馈修正；最多 8 轮充分性检查的总约束不变。

研究 policy 更新至 literature_review v4，删除固定综述章节要求。分类后的交付约束会保留移入的原文要求并去除完全重复项，上限容纳合法转移，保证保存的 plan.json 可重新加载。此次没有更换模型、关闭 RAG 或改变子 Agent 并行机制。

2026-09-12 同需求 WebSocket 实测完成：review_f62b4a4604b2459483201738039226b1。3 次审查中第 2 次触发语义复核且仍保持不足，后续读到复杂度表等证据后第 3 次通过，完成 brief 模板的 Markdown/TeX/PDF 交付。详见开发日志，不把一次成功等同于停止判断已全面可靠。

### 21.8 Skill 参考核对后的实现方向

源码与产品交互的对照记录放在 [Skill 设计对照](docs/skill-reference-study.md)。开发采用以下职责划分，不引入第二套科研编排：

- 平台 policy 固定维护权限、证据纪律和执行边界；用户偏好与可选 Skill 不能覆盖。
- 主 Agent 基于任务意图发现能力，子 Agent 只按自身目标和阶段加载相关指导；用户主动指定是任务配置，不是前端强制选择任务类型。
- 内容指导按任务类型、论证、比较、归因等维度逐步细分；格式审阅指导、模板资产、编译工具分别维护。不把某投稿场景的章节规范注入研究充分性审查。
- 当前内容 Skill + 辅助 guidance + 格式 contract + template profile 的组合保持不变。将来支持章节级写作时，再让 Writer 对当前段落发现子技能；不用预先固定“一章一个研究员”。
- 下一笔可选增量：输入区 `/skill` 复用现有指定接口、技能详情按主文件/参考资料分开展示；后续增加受审查的技能包导入和兼容性报告。社区包的工具依赖、运行目录、渲染器与许可证必须核对，不能把“注册成功”当成“即插即用”。
- 出版工具优先补规范化 BibTeX、引用标签和独立格式检查；它们作用于交付内容，不提高研究目标门槛。复杂数学与可编辑 LaTeX 项目另立增量。
