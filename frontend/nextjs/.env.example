# Local-only development mode. Keep disabled in shared/production deployments.
NEXT_PUBLIC_ASTERIA_DEV_AUTH_BYPASS=1

# Optional: point the browser and Next API proxy at a non-default backend.
# NEXT_PUBLIC_ASTERIA_API_URL=http://127.0.0.1:8000
