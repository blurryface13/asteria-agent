# Research 交付收敛（2026-09-25）

## 基线与不变项

保留 Coordinator → Lead → 并行 Search Subagents 的研究循环。Lead 决定覆盖是否充分；不增加研究审批角色。
Cookbook 复用委派/综合原文；独立 Analyst/Writer 参考 SDK demo，是明确的组合设计，不宣称生产系统完整复刻。
CitationAgent 专注正文与证据的引用对应关系，不重新制定研究目标或要求运行建议实验。

## 本轮工作

1. 版本/状态：记录 worker 启动版本，区分研究正文完成、发布中、文件已生成和数据库最终交付；旧失败任务不改为成功。
2. 证据：移除每段前 2400 字及全局 96 条的静默裁剪；引用按正文相关性选择完整片段，允许模型按 ID 补读，不以未展示等同不存在。
3. 图表：移除长单元格失败和字符硬截断；明确单元格对应来源，保持指标和协议，图表数据与 Writer 可读说明共同交接。布局调整不能更改数据事实。
4. 验证：针对旧 BadCase 回归，提交后重启空闲服务，使运行版本可追踪；原始复杂 CV 请求走真实登录→路由→批准→研究→引用→PDF→授权下载。
5. 验收：阅读正文与原始图表数据；逐页看 PDF。保留原目标，不把离线恢复/模型通过分数替代产品交付和人工质量判断。

## 当前起点

- ccd07471fa2c48bfbdbabacaf3937473 已失败：Lead 和三个并行子任务正常返回，图表列数正确，但旧 120 字符限制阻断交付。
- review_e6292cc574ed4586a797c362167b9f57 离线恢复在引用阶段超时。
- review_e0970a1b75224a229c01182dd6f3a2f3 离线 PDF 存在，不是新端到端成功。
- Consensus 待授权，不假装接通；金融长链路不在本次 CV 验收结论范围。

## 风险控制

不自动切换模型供应商，不公开凭据；只在测试账号/产物中验收。服务仍有任务时不重启。未完成的图表/引用明确报阶段状态，不用无来源占位图掩盖失败。

## 实现与回归

- CitationAgent 按当前正文批次检索完整证据，保留全目录并提供 `read_evidence_ids` 补读；移除原 96 条/2400 字静默裁剪。已有正文的修复仍限制在缺口位置，不重新启动全研究。
- 图表新增单元格证据映射、单指标柱图契约；长标签经展示编辑后仍保留全文，不做字符切断。上述来源校验不等于自动证明语义正确，仍需对最终报告人工验收。
- `run.json` 的成功改为 `research_completed`；`delivery-state.json` 记录产物 pending/publishing/ready/failed/cancelled；数据库 `research_runs.status` 才是用户任务最终状态。每次 worker 执行记录启动版本事件。
- 完整本地回归 346 passed / 5 skipped（需要显式外部环境的测试）；另显式启用隔离 PostgreSQL 生命周期测试 3 passed；前端类型检查通过、6 项 Node 测试通过。
- 前端继承原未提交的图表鉴权加载、下载完整报告包修改；按现有产品规范只补交付入口，不改变工作台布局。
- 新真实验收 `10a5069ab5854697abc6d1454ef58d74` 已失败，不能记为通过：运行版本为干净的 `31ee3c2`，研究已完成，图表摘录校验阻断发布。15 个实际读取来源、6 个子任务；资料目录与已读证据分别统计，不把下载候选计为已读。

## 本轮续跑与状态审计

- `76aeaad`：统一 PDF 排版引号的等价形式，不改变原文内容；恢复时复用已完成的 Lead handoff。
- `7222fee`：可选数值图缺少支持数值的摘录时，保留三张有效定性矩阵，明确记录被拒图及原因，不把无依据数字画出来。
- `cf64247`：引文“全目录可引用、仅当前片段可校验”的契约冲突已修复。引用枚举限定完整已展示证据；其余通过补读取得，补读同时带回该来源已保存的邻近片段。未展示的已知 ID 先读取再重新判断，不直接放行；真正不存在的 ID 仍拒绝。
- Writer 把内部 `e_…` 指针误写为 `KB:` 引用的情况，在已核对正文输出阶段替换为正式来源链接；真正私有知识库标记不受影响。
- `review_05b8871fbdb949439b29c1390479cd06` 离线恢复失败于补读预算耗尽，原始失败保存。`review_72404a41d0ec4794bd47bd74a8a058fd` 进程中断，未产出 PDF；其初始 recovery.json 没有终态，不能据此认为仍在运行或已成功。
- 恢复脚本现在在模型调用前保存 checkpoint、阶段、PID 和开始时间；PID/事件与文件状态一起检查，文件中的 running 不是进程存活证明。
- 当前回归：350 passed / 5 skipped；旧依赖弃用警告不影响测试结果。新恢复与真实端到端验收结果继续分别登记，不能互相替代。

## 引文恢复、PDF 与图文质量复核

- `review_8652a18745fa47d5ba4817dcc5014730`：11 次模型调用后完成 CitationAgent，原恢复在 PDF 阶段失败。原因是安全 LaTeX 转义把公式 `\\%` 变成裸 `%`，注释掉数学闭合符；不是检索或引文失败。
- `245284a` 修复数学字面符号；`publish-reviewed-report.py` 可只重试已经完成引文校验的正文发布，不再重复研究/写作/裁判模型调用，结果另存 publication-recovery 文件，原失败不变。
- 仅发布恢复产物：`outputs/delivery_recovery/review_8652a18745fa47d5ba4817dcc5014730/scientific_4ad77cdf85324bf7be1439f17d3e4ae2/report.pdf`，9 页、3 张定性矩阵、16 个参考条目。逐页检查中文、数学、图表及编号；补修 `≠`、`\\eta`、参考编号双重显示。无 Missing character/Overfull/invalid math 警告。
- **质量仍有缺口**：图中明确列出了机制，旧正文却复述规划阶段的“大量未报告”；把有限阅读范围推成全领域“无基准”的结论过强；正文来源数量与书目数量不一致。CitationAgent 通过不是这些判断正确的证明。当前 PDF 可读，但不能据此宣称内容验收全部通过。
- `c1a39ff` 调整通用 Analyst→Writer 交接：交付实际渲染单元格、完整行描述及来源，移除可能过期的规划 rationale/caption，不手改这份报告掩盖 BadCase。没有增加新的 Reviewer 或研究审批循环。
- 最新完整回归：353 passed / 5 skipped。真实 XeLaTeX 百分号公式测试包含在内。
- 浏览器：独立旧测试账号登录、历史任务加载通过，旧失败状态和各子任务回传记录一致。
- 新真实验收 `3de1d30bd8c6482e9740c728dff8e8e0`，目录 `outputs/acceptance_db4f2fe41c`，使用相同复杂 CV 请求，从真实登录/路由开始，worker 记录干净版本 `245284a`。运行期间不重启它；后提交的图文交接优化不冒充已在该 Run 生效。
