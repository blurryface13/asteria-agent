# 金融长报告首轮真实验收（2026-09-25）

任务：比较 NVIDIA 投资者关系官网 FY2026 与 FY2025 年报中的收入、经营活动现金流，说明期间、币种、单位，并分析一项主要风险；交付带邻近引文的中文 Markdown/PDF，不提供投资建议。

## 运行证据

- 命令：`/Users/dora/miniconda3/envs/dora/bin/python scripts/smoke-financial-report.py --max-actions 48`。此脚本为收费模型的显式冒烟，自动确认测试范围；不作为 CI 测试。
- Lead 派发两个互补子目标：财务报表数据原文提取、风险因素原文核查；39 次模型调用、27 次行动、2 个子 Agent，研究与引文阶段约 92 秒（不含 PDF 编译）。
- 已读原文为 NVIDIA 投资者关系官网链接的 FY2026 Form 10-K PDF，共 93 页；原文文本中检出收入 215,938、经营现金流 102,718（百万美元）及供需错配风险句。SEC EDGAR 页面在当前网络返回 403，未将其当成已读证据。
- Writer 交付中文 Markdown；CitationAgent 审查 45 个正文事实行，记录 0 个未解决缺口；XeLaTeX 生成 5 页 PDF。正文引用放在对应论述附近，并区分 FY2025 比较列与未单独读取 FY2025 10-K。
- 本地可检查：`outputs/review_d0dd8845a805451782f03d06a5c53c2c/`（轨迹、证据与引文审查），`outputs/scientific_870d39535605435f831ae0ea047090fe/report.md` 与同目录 `report.pdf`。`outputs` 不提交 Git。

## 人工复核与限制

- 报告的收入和经营现金流数值、期间与单位已从保存的原文再次比对；约 65.5%／60.3% 的增幅是基于原始两年数字的计算值，不是公司直接披露的增长率。
- CitationAgent 的“45 行支持”是模型判断，不是独立财务审计。该报告只使用一个官方 10-K，未交叉核对 FY2025 原始年报、SEC 版本或 MD&A；不能据此声称跨来源全面调研通过。
- 子 Agent 两次 `finish` 结构校验失败后在预算内修正并完成；应保留此失败轨迹供后续提示词与结构化输出回归。
- 使用的是直读页段模式；尚未验证 4060 的 Ollama `bge-m3`、多人并发、知识库金融数据、认证前端发起任务与容器化部署。下一轮测试需补至少一个行业报告及一个非美国市场任务。
- 完整报告脚本中的公开金融检索经本地只读 MCP 执行；另单独调用 MCP，取得 5 个官方候选链接（`investor.nvidia.com`、`www.sec.gov`）。尚未从认证 UI → Worker 全链路重跑，不将脚本冒烟视为前端验收。

## 工具与 Anthropic 参考边界

Anthropic `financial-services` 仓库的行业概览与模型更新 Skill 保留为分析指导；其商业数据连接器需要各自授权，不随本项目自动可用。Asteria 新增本地只读金融资料 MCP，使用公开搜索定位白名单内官网 URL，不需要金融数据 API Key；阅读原文和引文核查由已有 Tool/Agent 完成。SEC 的公开 EDGAR 数据 API 本身不需密钥，但本机对 SEC 网页的访问被拒绝，未绕过访问限制。
